# Phase 0-2 ランタイム依存 (ローカル開発・self-play 評価用)
# 各バージョンは Kaggle Notebook 環境 (2026-05 時点) と整合性を取る

# Kaggle 公式 CLI と環境
kaggle>=1.6.0
kaggle-environments>=1.28.0

# 数値計算 (heuristic / search 用)
numpy>=1.26
scipy>=1.11

# Phase 3 NN value/policy head (H016〜)。ローカルは CPU forward / ONNX export 検証のみ
# (GPU 学習は Kaggle Notebook に委譲)。ONNX runtime version は提出統合 step で Kaggle に pin。
torch>=2.2,<2.6
onnx>=1.16
onnxruntime>=1.18

# 開発ツール
ruff>=0.4.0
black>=24.0
pytest>=8.0
pytest-xdist>=3.5  # 並列 self-play match
