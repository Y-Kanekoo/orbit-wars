# Hypotheses Backlog

仮説バックログ。`researcher` subagent が補充し、`implementer` が優先度順に消化する。
B-7 (Phase 0 完了基準) として ≥ 10 件の populate が必要 (PLAN.md L933)。

## スキーマ
| 列 | 説明 |
|---|---|
| id | 連番 (`H001`, `H002`, ...) |
| status | `active` / `in_progress` / `done` / `discarded` / `stuck` |
| phase | 該当 Phase 番号 (`1`, `2`, `3`, `4`, `5`) |
| priority | 1-5 (5 が最高) |
| effort_hr | 想定所要時間 (時間) |
| expected_gain_lb | 期待 LB 上昇値 |
| hypothesis | 仮説本文 (1-2 行) |
| validation_method | 検証方法 (例: `vs Phase 1 self-play N=100, winrate≥55%`) |
| refs | 参考リンク・lessons |

## Active

| id | status | phase | priority | effort_hr | expected_gain_lb | hypothesis | validation_method | refs |
|---|---|---|---|---|---|---|---|---|
| H000 | done | 1 | 5 | 4 | +0 (基盤) | 移植 parity 修復: exp/000b で main_bare.agent への DUMP 仕込み → env.run 経由で agent が呼ばれていない疑い (file 作成されない、直接呼びでは作成)。仮説は「import 共有」より更に上流「agent loader の関数解決」へシフト。次イテレーション入口: kaggle_environments.envs.orbit_wars の source 確認 + main_bare の global を消して毎ターン write 検証 | parity check pass (winrate 0.40-0.60 N=30) | learned_rules.md AVOID phase1_baseline_port_parity_skip / mirror_match_shared_src_import / INFO main_bare_agent_not_invoked_in_env_run; exp/000-parity-fix branch |
| H021 | done | 1 | 5 | 4 | +0 (基盤) | mix-eval infra: tournament.py に `--opponents random,nearest_sniper,prev_best` 追加 (N=30/相手, seat split)。best_score.json schema v2 で mix_eval section。kaggle-quota-guard hook + submit.sh に submit gate enforce。**全 hypothesis の prerequisite** | submit gate (no-regression) = winrate_random≥baseline_random AND winrate_sniper≥baseline_sniper AND winrate_prev_best≥baseline_prev_best+0.05 AND winrate_min≥0.50 AND errors_total=0。baseline は best_score.json mix_eval から動的読込 (hardcode 回避) | exp/021 + exp/021b 完了。baseline mix_eval: random 0.8667 / sniper 0.5667 / prev_best 0.5667。gate を絶対値 (random≥0.90) から no-regression に変更 (絶対値だと baseline 自身が通らない問題、supervisor 通知2) |
| H024 | discarded | 1 | 5 | 4 | +60 | beam forward sim の **fleet projection 修正** (beam.py L488-490): 敵 fleet を target=-1/turns_remaining=99 固定でなく、最近接 enemy planet を target 推定し ETA で探索内到達させる。**最小実装** (target 推定のみ、他 knob は baseline 据置) | **2026-05-24 relaxed gate で active 復活**。前回 exp/024 discard は prev_best gate (+0.05、mirror で LB 非予測) が原因だった。新 gate (prev_best を block から除外、random/sniper no-regression + winrate_min(random,sniper)≥0.50) では H024 の random 0.9333↑/sniper 0.6↑ が **pass する見込み**。再実装し gate pass で **即 submit** (4 月以来初の LB 更新)。wins 一致 (28/18/14) は mirror noise と解釈 | advisor 助言。前回 INFO `fleet_projection_freeze_not_root_cause` は「dominant でない」程度に弱め、relaxed gate 通過後の LB で真の効果を測る。**【2026-05-25 discard】** exp025 提出 52989108 の LB = **378.2 (-36 vs baseline 414.2)** で regression 確定。beam.py を frozen baseline に revert、fleet projection 路線 打ち切り (mix-eval gate PASS は LB 改善を保証しないことを追認) |
| H023 | discarded | 1 | 5 | 3 | +0 (診断→depth unlock) | beam の opponent model を beam-at-depth-1 argmax (`_opponent_best_actions`) に強化し depth=3 で test。深い探索が誤った opponent モデルに過適合する問題の直接テスト | **exp/023 で discard (診断完了)**。depth=3 + 新 opponent model: random 0.8667→0.9333↑ / nearest_sniper 0.5667→0.6↑ / **prev_best 0.4667** (H004 depth=3+旧 opponent 0.4333 から +0.0334 ≒1 試合のみ改善、baseline 0.5667 / gate 0.6167 に遠く届かず)。winrate_min 0.4667<0.50 で gate FAIL。**診断結論: opponent decision model は helps-weak/hurts-strong の主因でない** (強化しても regression 残存)。真因は downstream (fleet projection の target=-1/turns_remaining=99 凍結 or eval)。learned_rules INFO opponent_model_not_root_cause_of_depth_regression | advisor 助言。beam.py は baseline (depth=2, `_phase1_decisions` opponent) に revert。次手: H011 PUCT-MCTS は opponent model 改善より fleet projection 修正 (target 推定) を先に検討すべき |
| H001 | discarded | 1 | 5 | 3 | +80 | territory control map を eval に加算 (係数 0.3) | (再評価は H002+H003 land 後の combined base 上で mix-eval winrate_min≥0.50 必須、単体 sweep 禁止) | exp/001 + exp 002 で discard。exp 002 は LB regression -17.3 (ローカル 63.3% でも LB 悪化)。learned_rules.md INFO single_opponent_winrate_not_lb_predictive。**【2026-05-28→2026-06-03 supervisor resurrect 試行→再 discard】** exp/002 submission 52716957 の成熟 LB 423.2 (旧 baseline 414.2 比 +9) を根拠に supervisor が exp/027 で resurrect、submission 53123447。109h 後の LB **394.4 で新 baseline 435.3 比 -40.9** と明確 regression → PR #58 close + ledger exp 027 discard 追記。教訓: resurrect 根拠が旧 baseline (state lag) ベースで、提出時点で既に新 baseline (53126719/435.3) が Kaggle 上に存在していたのを見逃した supervisor 判定ミス (`supervisor_resurrect_against_stale_baseline`) |
| H002 | discarded | 1 | 5 | 4 | +100 | 30 turn 先までの ship 在庫 projection (production + incoming fleet 減算) を eval に加算 (係数 0.3-0.8 sweep) | exp/002-projection で discard。projection_total はスカラー一律加算で係数 sweep 無効 (0.3/0.1 で prev_best 完全同一 0.3667)、production 二重カウントで強相手 regression。learned_rules.md AVOID eval_term_redundant_with_production | PLAN.md L268; projection.py 実装は残置 (default off)、再定式化は H022 |
| H003 | discarded | 1 | 5 | 3 | +120 | 敵 fleet の進行方向考慮 threat (incoming_threat_eta) を eval 守備減算項に統合 (旧 incoming_threat*1.5 を eta*2.5 に置換) | exp/003-threat-eta で discard。mix-eval: random 0.9667↑ / sniper 0.5333↓ / prev_best 0.4667↓、winrate_min 0.4667<0.50 で gate fail。learned_rules.md INFO new_eval_term_helps_random_hurts_strong | incoming_threat_eta + tests は休眠 infra 残置 (eval 未統合、beam.py は baseline 復帰)。再定式化 (別 weight / baseline 併用) は H007 grid search 対象 |
| H022 | discarded | 1 | 4 | 5 | +60 | projection 再定式化 (eval 項追加) | — | **eval 項路線 discard**: H001/H002/H003 が 3 連続で strong opponent regression (new_eval_term_helps_random_hurts_strong)。defer でなく discard で priority sort 再浮上を防ぐ。MCTS で強 baseline が出た段階でのみ再評価検討 |
| H004 | discarded | 1 | 5 | 6 | +60 | beam tuning (search 改善, eval 不変)。**exp/004 で discard**。実証: budget/width は no-op (depth=2/width=16 で beam は max 6ms/budget 300ms、width sweep 16/24/32 は frontier 飽和で timing 完全同一 = a/b/c skip)。唯一の実効レバー depth 2→3 (H004d) を実施 → mix-eval: random 0.8667→0.9333↑ / sniper 0.5667→0.6↑ / **prev_best 0.5667→0.4333↓** (winrate_min 0.4333<0.50 で gate FAIL)。深い探索が誤った opponent モデル (`_phase1_decisions` heuristic) へ過適合し強い相手で regression。eval 項に続き **search depth でも helps-weak/hurts-strong**。learned_rules INFO `deeper_search_overfits_wrong_opponent_model` | mix-eval no-regression gate | beam.py/main.py は baseline (depth=2, budget 0.3) に revert。depth 改善は正しい opponent モデル (H011 MCTS / H013 classifier) 確立後に再検討 |
| H005 | discarded | 1 | 4 | 4 | +50 | sun blocking aware path planning: avoidance_angle の単一 12° stub を robust 化 (偏向後経路が sun を抜けるまで段階的に増やす)。fleet が straight-line 移動かつ sun-crossing で破壊される env 仕様に対し、現 stub は 12° で抜けない near-tangent 経路で fleet を sun に捨てる。出力 move の角度のみ変更 (beam 内部 sim 不変) で bounded downside | mix-eval no-regression gate (random≥0.8667 / sniper≥0.5667 / winrate_min≥0.50 / prev_best≥0.35 / errors=0) | PLAN.md L271, B-12。**exp/031 で discard**: avoidance robust 化 (12°→段階増加 90°) は探索/eval 非関与の別カテゴリと予測したが、mix-eval 2 回連続測定で random 0.8667 (不変) / sniper 0.5333 (16勝, baseline 0.5667=17勝 から **-1 試合 regression**) / prev_best 0.6↑。winrate_min 0.5333 は OK だが sniper no-regression 違反で gate FAIL。effect は noise レベルで gate を 1 試合差で割る。geometry.py を baseline に revert (parity 維持)、no-submit。learned_rules INFO `avoidance_robustification_noise_level_no_gain` |
| H006 | discarded | 1 | 4 | 5 | +50 | comet pre-positioning → **reactive comet targeting に pivot** (exp/039)。env source 実測で確定: COMET_SPAWN_STEPS=[50,150,250,350,450] は固定だが spawn **位置は予測不能** (orbit_wars.py L347 `episode_seed` を configuration から scrub し「Agents must not be able to reconstruct the comet schedule」)。as-written pre-positioning は設計上不可能。pivot: comet は spawn 時に neutral planet (owner=-1) として `comet_planet_ids` で可視化されるが eval で**完全に無視**されている (src/eval で comet 参照ゼロ)。`score_target` (targeting.py) に comet target bias を追加 (additive eval scalar でなく target 選択軸 = `eval_term_redundant_with_production` 回避、advisor 助言) | vs baseline N=30 mix-eval no-regression gate (LB が真の judge、N=30 は noise sanity) | PLAN.md L270; advisor 助言 (env source 実測→reactive pivot)。**【exp/039 discard】** COMET_TARGET_BONUS=6.0 で comet 可視時に target 選択 bias を付与。mix-eval (fresh): random 0.9333(28) / sniper 0.6(18) / **prev_best 0.3667(11)** / errors=0。stale legacy baseline では gate PASS だが、真の noop-beam baseline (0.9667/0.6/0.7) では random -1g で no-regression FAIL かつ prev_best 0.7→0.3667 collapse、**全相手で upside ゼロ** (random↓/sniper=/prev_best↓)。comet は安価/近接時のみ tip する bounded bias だが mix-eval games で comet が決め手になる頻度が低く `avoidance_robustification_noise_level_no_gain` と同型 = no-gain。agent を baseline に revert (bit-identical, 提出 435.3 維持)、no-submit |
| H007 | discarded | 1 | 4 | 6 | +50 | eval 係数 grid search: threat 係数 0.5-1.0 のみ補強 (territory/projection は discard 済で off 据置、eval 項路線回避)。H004 の fallback | best vs baseline、mix-eval no-regression gate | PLAN.md L272; H004 が gate FAIL or LB<+30 時の次手。**【exp/056 で discard (実装前、category-kill)】** 対象は beam.py:452 の既存 `incoming_threat*1.5` 係数 sweep。eval-term 単一 knob route は `new_eval_term_helps_random_hurts_strong` / `eval_term_redundant_with_production` で kill 済 category (H022 が同理由で実装前 discard した先例)。加えて `mixeval_prevbest_gate_noisy_misjudges_lb`+`tournament_noise_from_wallclock_cutoff_under_parallel_load` により **N=30 では byte-identical baseline すら 29↔27/21↔15 振れ、単一 knob 効果を統計分離不能** = 係数 sweep の go/no-go を local で測れない。agent-modifying ゆえ infra_merge 不可・判定には submit が必要だが kill 済 route に quota を焼く価値なし。priority-sort 再浮上を防ぐため discarded 昇格 (H022 先例)。escape は NN (H016) のみ |
| H008 | discarded | 1 | 4 | 3 | +30 | beam の action_gen で空 action set `[]` も必ず候補に含める ("do nothing" 評価) | vs H004 N=30 winrate>50% (= 棄損しない) | PLAN.md B-8。**exp/030 で discard (既実装と判明)**: beam.py `_candidate_actions_for_planet` L213 で `wait = Action(target_id=None, ships=0)` を定義し L256 `ranked + wait` で全候補に常時含めている。"do nothing" は既に評価済みのため新規実装の余地なし (no-op) |
| H009 | discarded | 1 | 4 | 5 | +60 | multi-launch per planet: 1 turn の compound action を最大 K=3-5 launch list で生成 | **discarded (LB evidence, exp/039 bookkeeping)**: 別 actor が同仮説を提出 (kaggle submission 53173411 'exp 032 H009 multi-launch', 2026-05-30) → LB **403.4 < baseline 435.3** で regression 確定。再実装は無意味。priority-sort 再浮上を防ぐため discarded 昇格 (advisor 助言、scope creep でない bookkeeping) | PLAN.md B-3; best_score.json notes 未解決(1) の untangle |
| H010 | discarded | 1 | 3 | 4 | +40 | depth-1 defensive minimax (自分 1 手 → 敵最悪応手) を eval に組込 | vs H003 N=30 winrate≥55% | PLAN.md B-12。**【exp/057 discard — 実装前 category-kill, opponent-model 路線 = LB 否定済】** H010 の「敵最悪応手」は探索内 opponent-response modeling であり、(1) 現 beam は `_simulate_opponents` を **noop 化** (beam.py:538-542) した H025/exp029 が **LB +21.1 の唯一の実改善** (414.2→435.3) = 「探索内で敵を静止扱い」が LB で勝った direction、H010 はその逆戻し。(2) H023 (`opponent_model_not_root_cause_of_depth_regression`) が既に「敵最悪応手 (`_opponent_best_actions` minimax 応手)」を直接テスト済 → prev_best 0.4667 で inert/gate FAIL、H010 は eval 項 vs rollout step の違いだけで同一方向。(3) defensive minimax は候補 state ごとに敵応手生成+評価で高コスト、dense turn は既に 0.3s cap 律速 (`beam_budget_underutilizes_acttimeout`)、`actTimeout_full_use_regresses_strong_opponent` で compute 増は LB regression 確定。(4) N=30 prev_best 真値 0.51・識別力ほぼゼロ (`mixeval_prevbest_gate_noisy_misjudges_lb`) で実装・測定しても submit を正当化不能。H007/H022 と同型の priority-sort 再浮上防止 discard。agent byte 不変 (no code change)・no-submit・state-only merge。learned_rules `opponent_response_modeling_lb_disproven_noop_won` (新規) |
| H011 | discarded | 2 | 5 | 8 | +150 | PUCT-MCTS、rollout 200-400 (1秒予算)、rollout policy = Phase 1 heuristic | vs Phase 1 best N=50 winrate≥65%、LB ≥950 | PLAN.md L294, 303。**exp/028 で minimal 実装→ relaxed gate FAIL → discard**: random 0.8667→1.0↑ / sniper 0.5667→0.20↓↓ / prev_best 0.5667→0.1667↓↓、winrate_min(random,sniper)=0.20<0.50。helps-weak/hurts-strong 6 系統目、パラダイム置換でも症状悪化。learned_rules INFO `paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` 昇格。src/search/mcts.py は default OFF (`ORBIT_WARS_MCTS=1` 切替) で残置 (H012/H014 等の MCTS 系後続改善の infra として)、main parity (baseline 414.2) 不変 |
| H012 | discarded | 2 | 4 | 4 | +50 | MCTS progressive widening (k=0.3-1.0 sweep) | best k vs default k N=30 winrate≥55% | PLAN.md L304。**【exp/051 discard — PW 構造的 inert】** PW (prior=`_score_state` 上位 child を `ceil(k·N^α)` で段階解禁、env `MCTS_PW_K`) を実装・pytest 73 pass。機構 probe (5 seed dense obs) で **PW が本 MCTS regime で no-op と確定**: root child 数 (beam frontier) は **1〜6 個**で BEAM_WIDTH=16 未到達 (owned-planet 律速)、0.3s budget ~400 sims で過剰カバー、visit 分布は pw_k=0/0.3/0.5/1.0 で**完全不変** (6-child seed で flat top1=0.17)。PW は「多数 child 薄分散」病に効く手法だが本 MCTS は少分岐ゆえ visit 不足でなく **value-tie** が実態 = `paradigm_swap...` の「root-only 薄分散」候補を精緻化。winrate A/B は visit 分布 byte-identical (= 選択 move provably 同一 → winrate=exp028) のため冗長と判断し省略。agent byte 不変 (MCTS env OFF)・gate PASS だが no-submit infra_merge。次手: MCTS は木の深化 (multi-ply) / eval×sigmoid 再設計を優先、PW/RAVE 系 (H014/H015) も同型 inert 可能性。learned_rules `mcts_progressive_widening_inert_small_branching` |
| H013 | discarded | 2 | 3 | 6 | +70 | offline opponent classifier (expansion/aggression/comet_priority 3-class) + online dispatch | vs MCTS w/o dispatch N=30 winrate≥55% | PLAN.md L308。**【exp/058 discard — 実装前 category-kill, 3 系統の独立 kill 理由】** opponent classifier は 3 つの独立した既消込ルールに同時抵触: **(1) opponent-modeling = LB 否定済**: learned_rules `opponent_response_modeling_lb_disproven_noop_won` の防止策が「opponent-modeling 系 hypothesis (eval/rollout/**classifier** いずれの形でも) は priority-sort 再浮上時も着手前に discard」と classifier を名指し。現 beam の `_simulate_opponents`=noop (敵を静止扱い) が baseline 414.2→435.3 (+21.1) = 唯一の実 LB 改善の源泉であり、敵タイプを分類して読み筋を変える方向はこの軸の逆行。**(2) dispatch 先が存在しない**: H019 (戦況 dynamic dispatch) が `blocked-on-phase3` の理由「切替元となる viable な代替戦略が beam(noop) 1 本のみ = dispatch 不能」と同根。online dispatch の切替先候補 (depth/eval/opponent-model/fleet/paradigm/budget の beam 変種) は単一 knob kill 12 系統で全滅済 → 分類しても切替先が全て LB regression。**(3) offline classifier の学習データ生成が local 不能**: opponent-type ラベル付き self-play data は `local_cpu_selfplay_export_infeasible_at_scale` (~74s/game CPU, 数千 game=20h+) で生成不可、Kaggle GPU escalate (supervisor gating) 領分。H007/H010/H022 と同型の priority-sort 再浮上防止 discard。agent byte 不変 (`git diff main -- main.py src/` = 0 行, 提出 435.3 維持)・no-submit・state-only merge。pytest 73 pass。learned_rules `opponent_classifier_dispatch_triple_killed` (新規) |
| H014 | discarded | 2 | 3 | 6 | +30 | MCTS transposition table (Zobrist hash) で同一状態再訪コスト排除 | rollout/sec が 1.5-2x | PLAN.md A-16。**【exp/053 discard — TT premise 構造的に不能】** learned_rules `mcts_progressive_widening_inert_small_branching` の指示「着手前に分岐数 probe で premise 確認」に従い `scripts/selfplay/probe_mcts_transposition.py` で機構 probe (5 seed × 4 step)。**決定的: 現 MCTS は root-only depth-1 木 (children を孫展開しない) で各 child は固有 action set 適用後の distinct state → 木内 transposition が原理的にゼロ**。実測: 163 root child 全てで within-tree transposition **0 件** / tt_hit_opportunity=false。TT は「同一 state 再訪コスト排除」を謳うが再訪自体が起きない木構造 = H012 PW inert と同型 (より根本的に不適用)。唯一の冗長性は phase1 rollout 決定論性 (同一 child 再 rollout が同値) だが TT でなく memoize の対象、かつ H026 で rollout は strong-opponent regression の root cause でないと確定済。agent byte-identical (probe は scripts/ のみ、main.py/src 改変 0)・no-submit infra_merge。learned_rules `mcts_transposition_table_inert_depth1_tree` |
| H015 | discarded | 2 | 3 | 5 | +50 | RAVE / AMAF rollout 共有 | vs base MCTS N=30 winrate≥55% | PLAN.md L309。**【exp/056 discard — premise 構造的 inert】** probe-first (3 seed × 4 step, `scripts/selfplay/probe_mcts_rave.py`) で RAVE inert を 3 系統の独立理由で確定: **(P1 不成立)** 分岐は dense で 16 (BEAM_WIDTH cap) 到達するが最悪 avg_real_visits/child=32・sparse 467-2082 = 全 child 十分 visit、AMAF early-estimate 不要。**(P2 signal あるが非識別)** cross-child 再出現は非ゼロ (4-208, TT と違い共有機構は動く) だが distinct_rollout_actions=0-5 (phase1 決定論 greedy で action universe 退化)、少数 action が全 child に一様出現 = AMAF は全 child 同一 bonus で順位不変。**(決定打) child_value_spread=0.0-0.0068** = value-tie ゆえ AMAF 並べ替えても argmax 不変 (H012 追認)。PW/TT/RAVE 全 inert の共通根 = root children の value-tie。escape は eval/value 質 (NN H016) or 木の深化 (multi-ply)。learned_rules `mcts_rave_inert_value_tie_degenerate_rollout` 昇格。agent byte-identical・pytest 73 pass・no-submit infra_merge |
| H016 | in_progress | 3 | 4 | 16 | +100 | Transformer encoder (planets+fleets を token、4-layer/d=64) + NN value head のみ (policy は MCTS visit dist) | vs Phase 2 best N=30 winrate≥55%、LB ≥1100 | PLAN.md L328-334, L349。**exp/032 で infra step 1 着手 (strategic pivot)**: 8 系統 (eval×3/depth/opponent/fleet/paradigm/rollout) 全て helps-weak/hurts-strong で kill、learned_rules 残候補 (c) eval×sigmoid は提出物 beam に sigmoid 無しで崩壊 → live escape は NN のみ。締切 06-23 (残24日) で着手必須。今 iter は agent 不変・no-submit の +0 基盤 (H021 先例): state→tensor encoder のみ (`src/features/encoder.py`)。実学習/Kaggle GPU は後続 iter。**exp/033 で step 2 完了**: self-play value data exporter (`scripts/selfplay/export_value_data.py`) 追加。trajectory の各 timestep を encode_observation → value=rewards[player] ({-1,0,+1} player 視点勝敗) で npz 化。観測は推論時と同一の parse() 再利用 (train/serve skew 回避)、符号正当性は --smoke で勝者+1/敗者-1 を assert。agent 不変・gate PASS だが no-submit (infra_merge, exp/032 先例)。**exp/034 で step 3 着手 (NN value head module)**: `src/nn/model.py` に ValueNet (token embed + 4-layer transformer d=64/heads=4 + masked mean+attention pool → tanh value) を定義 (210K params, PLAN ~500K spec 内)。torch/onnx/onnxruntime を requirements 追加 (ローカルは CPU forward/ONNX 検証のみ、GPU 学習は Kaggle)。smoke で (1) encoder 出力 batched 消費 (2) **mask 適用検証** (padding に garbage 注入で出力変化 0.0) (3) **ONNX round-trip** (torch vs onnxruntime max_diff 1.3e-7) を assert。main.py は import せず byte 不変 → mix-eval baseline 同一。**exp/035 で step 4 着手 (学習 harness)**: `scripts/nn/train_value.py` を追加 (npz dataset → ValueNet を MSE 学習 → torch checkpoint + ONNX export)。Kaggle GPU で本番学習、ローカルは tiny synthetic data で CPU smoke。**val split は game 単位 (grouped)** で leakage 回避 (MC-outcome は同一 game の全 timestep が同一 value target、timestep random split は val loss を楽観化させ汎化判定を壊す → advisor 指摘)。前提として `export_value_data.py` に `game_id` を追加し npz に保存。agent 不変・no-submit (infra_merge)。**exp/036 で step 5 着手 (ONNX quantize)**: `scripts/nn/quantize_onnx.py` を追加。当初 plan note の「FP16」を **int8 dynamic quantization に変更** (advisor 指摘): 提出は CPU・1秒/turn 制約下で走り、ONNX Runtime CPUExecutionProvider は FP16 ネイティブ kernel が乏しく Cast(FP16↔FP32) 挿入で latency が縮まらない (むしろ悪化)。Linear/MatMul 主体の ValueNet は int8 dynamic が CPU で size↓ + speedup 本命。smoke は **CPU 推論 latency (FP32 vs int8) を必ず計測** (このステップの存在意義 = NN を 1秒/turn 内に載せられるか) + size 半減 + **sign agreement** (MCTS は value で state ranking、abs diff より符号保存が本質、tolerance は int8 現実値) を assert。agent 不変・no-submit (infra_merge)。次 step: MCTS value 統合。**exp/037 で step 6 着手 (MCTS value 配線)**: `src/nn/value_infer.py` (ONNX value 推論ラッパ) + `encode_search_state` (SearchState→EncodedState、root obs を parse() して comet/angular_velocity を thread し train/serve skew 最小化) + mcts.py の leaf eval を env flag (`ORBIT_WARS_NN_VALUE=1` + `ORBIT_WARS_NN_VALUE_MODEL=<onnx>`) で rollout+score → NN value に切替。agent 不変 (MCTS は env OFF、main.py byte 不変 → 提出 parity)・no-submit。**重要 (advisor 助言)**: ローカル CPU export は ~74s/game (probe 6 games=443s) で実用規模 (数千 game) は ~20h+ と非現実的。実 self-play data 生成 + Kaggle GPU 学習は **supervisor へ escalate (gating item)**。配線契約固定済のため実モデルは後で差すだけ。NN value が MCTS の strong-opponent regression (exp028 sniper 0.20/prev_best 0.1667) を治すかが H016 のコア signal、これは実モデル到着後に mix-eval で測る。**exp/041 で step 7 着手 (Kaggle GPU 学習 notebook + データ生成並列化)**: `local_cpu_selfplay_export_infeasible_at_scale` の真因 (data-gen ~74s/game CPU、しかも `export()` が完全 single-thread で Kaggle 4-core を 1 つしか使えない) を除去。(1) `export_value_data.py` に `--workers` multiprocess 並列化を追加 (game は独立、ProcessPool で fan-out、game_id は task 順で dense 再採番し決定的、workers=1 既定で従来挙動不変)。(2) `kaggle_notebooks/nn-value-train/` に parallel export→train→ONNX→int8 を end-to-end で回す script kernel を追加 (workers=cpu_count、first-signal 最小 ~500 game を 1 run で生成)。agent 不変・no-submit (infra_merge)。**GPU run は supervisor の gating (advisor 助言: runnable artifact + feasibility 試算 + 起動手順を handoff、multi-hour run は本 iter に入れない)** 。**exp/049 で cycle 5 着手 (in_progress)**: cycle 4 で生成・merge 済の `value_dataset_merged_c1_c4.npz` (810 games = cycle1 mix 510 + cycle4 legacy-388 300、value balanced -1:80428/+1:84928) で local CPU train → int8 quantize → NN-in-beam (`ORBIT_WARS_NN_VALUE_BEAM=1` weight=1.0) mix-eval 診断。go 基準 (cycle 4 plan): prev_best 0.6+ (cycle 2 0.4333 から +5g) で distribution shift 解消、なら agent 統合検討。未改善なら no-submit diagnostic discard。agent byte 不変 (env-gated)・no-submit。**【結果】** merged data で train (val_sign_acc 0.7045, int8 sign_agreement 1.0)、NN-in-beam weight=1.0 mix-eval: random 0.9333 / sniper 0.6 / **prev_best 0.50 (15/30)**。**distribution shift 解消を確認** (cycle 2 の prev_best 0.4333=-8g → 0.50 = N=100 baseline 真値 0.51 とほぼ同値に回復) = `nn_value_distribution_shift_legacy388_mirror` 真因の追認。**しかし gain なし (neutral)**: 3 相手とも baseline tie で go 基準 0.6+ 未達。加えて NN inference で tournament が ~26min (baseline 数分の ~6x) = per-turn latency 大、submission では 1s/turn DQ リスク。NN-in-beam additive (weight 加算) は dead-end 確定。次手: NN を beam additive でなく MCTS leaf eval (cycle 1 で 2x 改善) に戻す or value target/data の質向上。agent byte 不変・no-submit・infra_merge。in_progress 継続 (NN asset は `~/orbit-wars-models/cycle*` backup 済)。**exp/050 で cycle 6 着手 (in_progress)**: cycle 5 結論「NN-in-beam additive は dead-end、本命は NN を MCTS leaf eval (cycle1 で exp028 比 2x 改善実績) に戻す」を実行。merged-data モデル (`value_net_c5.int8.onnx`, distribution shift 解消済) を MCTS leaf path (`ORBIT_WARS_MCTS=1 ORBIT_WARS_NN_VALUE=1 ORBIT_WARS_NN_VALUE_MODEL=...`, exp/037 配線) に通し mix-eval 診断。**latency 安全確認 (hard requirement)**: c5 int8 を MCTS leaf に通し per-turn median 255ms / max 255.5ms (0.3s budget 律速、900ms hard limit に余裕)。cycle5 の latency 警告 (~26min/6x) は NN-in-beam additive 固有 (多数 state 評価)、NN-in-MCTS-leaf は wall-clock budget 律速で per-turn bounded → **1秒/turn に収まる**。go 基準: strong opponent で cycle1 MCTS+NN (sniper 0.47/prev 0.33) を merged モデルが上回り baseline 0.6/0.7 に近づくか。agent byte 不変 (MCTS は env OFF, 提出物 beam 不変)・no-submit 診断。**【結果: 統計 signal 取得不能 (local eval blocked)】** NN-in-MCTS leaf の N=30×3 mix-eval をローカル harness で完走できず。NN-in-MCTS は per-game wall-time 極大 (単発 match vs prev_best が 214s+ 継続、MCTS が 0.3s/turn budget をフル消費×数百turn)。tournament 親が ~466s で出力前に死亡 (OOM/reap)、workers絞り・単発並列・単発1本いずれもブロック (4 approach 全滅)。**per-turn は submission-safe** (255ms<900ms、commit 確認済) で提出 DQ リスクは無いが、go/no-go 統計判定はローカルで不能 → **Kaggle 側 eval に escalate (supervisor gating、`nn_in_mcts_leaf_local_eval_infeasible_in_harness`)**。agent byte 不変・no-submit infra_merge。in_progress 継続 (NN asset backup 済)。次手: Kaggle notebook self-play で NN-in-MCTS の strong-opponent winrate を測る、or NN value の質向上 (policy head H017 / 強teacher data)。 |
| H017 | in_progress | 3 | 4 | 8 | +80 | NN policy prior 追加 (AlphaZero 風 (value, policy_logits) 出力) | vs H016 N=30 winrate≥55% | PLAN.md L350。**exp/055 着手 (infra step)**: cycle 6 結論「NN value 自体の質向上 (policy head H017)」の next 手。model.py docstring 予告どおり `PolicyValueNet` を新規追加 (既存 ValueNet は checkpoint/value_infer 依存のため byte 不変)。policy action space = per-planet-token launch logit (MAX_PLANETS) + 1 no-op = (B, MAX_PLANETS+1)、planet_mask 適用、no-op 常時 valid。train target = MCTS visit 分布を launch 元 planet に集約 (Kaggle 学習側契約)。本 iter は infra (model 定義 + dual-output smoke + ONNX) のみ、agent byte 不変・no-submit infra_merge (H016 step 先例)。実学習/eval は Kaggle GPU escalate (`local_cpu_selfplay_export_infeasible_at_scale`)。**【exp/055 done (infra step)】** `PolicyValueNet` (210,244 params) 追加。dual output: value (B,) / policy_logits (B,17=MAX_PLANETS+1)。`_MASK_NEG=-1e9` で padding planet を masked (smoke: padding prob max 0.0、garbage 注入で valid logit 変化 0.0)、ONNX dual round-trip value 2.1e-7 / policy 3.1e-7。pytest 73 pass・ruff clean。agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/` = 0、PolicyValueNet は未 wire)・no-submit infra_merge。**次 step (Kaggle escalate)**: train_value.py に policy head loss (visit 分布の KL/CE) 追加 → MCTS root prior 配線 → winrate eval。in_progress 継続 (winrate go/no-go は Kaggle GPU 学習+統合後)。**exp/059 着手 (infra step)**: train_value.py に `PolicyValueNet` dual-head 学習 path (`--policy`) を追加 = value MSE + policy cross-entropy (MCTS visit 分布 target、masked logit safe) の combined loss。既存 ValueNet path は byte 不変 (checkpoint/value_infer 依存を保護)。CPU synthetic smoke で value/policy 両 head 収束 + dual ONNX round-trip を assert。実学習/eval は Kaggle GPU escalate (`local_cpu_selfplay_export_infeasible_at_scale`)。agent byte 不変・no-submit infra_merge。**【exp/059 done — infra step】** `train_value.py --policy` dual-head path 実装 (+343/-21 行)。smoke: combined loss 3.2156→0.7131 / policy CE 2.2326→0.7131 / params 210,244 / dual ONNX round-trip value 1.19e-7・policy 5.96e-7、既存 ValueNet path も byte 不変で収束 (train_loss 1.04→1e-6 / val 0.047)。pytest 73 pass・ruff clean。agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/` = 0)・no-submit infra_merge (exp/055 先例)。**次 step (Kaggle GPU escalate)**: 実 self-play data + visit 分布 target で dual-head 学習 → MCTS root prior 配線 → winrate eval。in_progress 継続 (winrate go/no-go は学習+統合後)。**exp/060 着手 (infra step — MCTS root prior 配線)**: exp/059 の「次 step」たる MCTS root prior 配線を実装。`src/nn/value_infer.py` に `PolicyValueModel` (dual-head ONNX 推論ラッパ、value+policy_logits、CPU intra_op=1) 追加 + `src/search/mcts.py` に PUCT 経路 (`_puct` / `_assign_child_priors` / `debug_root_priors`) を env-gate (`ORBIT_WARS_NN_POLICY=1` + `ORBIT_WARS_NN_POLICY_MODEL=<onnx>`) で配線。policy index 規約は train_value.py と 1:1 (i∈[0,MAX_PLANETS)=ships 降順 slot / i=MAX_PLANETS=no-op)、各 child prior は launch 元 planet の policy prob 平均→総和 1 正規化。policy OFF では従来 UCB1 と完全 parity (exp028 不変)。**【exp/060 done — infra step】** `_policy_smoke` で end-to-end 検証: untrained PolicyValueNet dual ONNX export → evaluate (value |v|<1 / softmax 総和 1 / padding slot prob 0.0) → MCTS PUCT 統合 (moves valid / prior 総和 1 / **spread 0.0465 > 0 = prior が value-tie を割り child を differentiate**、PW/TT/RAVE が割れなかった root-children value-tie を policy prior が割る AlphaZero 機構を実証)。pytest 73 pass・ruff clean。agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/` = 0、policy は MCTS env OFF default ゆえ提出物 beam 不変)・no-submit infra_merge (exp/055・059 先例)。**次 step (Kaggle GPU escalate)**: dual-head 実学習 (visit 分布 target) → NN-in-MCTS で PUCT prior + NN value leaf を併用し strong-opponent winrate eval (`nn_in_mcts_leaf_local_eval_infeasible_in_harness` ゆえ Kaggle 側 self-play eval、supervisor gating)。**exp/061 着手 (infra step — policy_target exporter, in_progress)**: 上記「dual-head 実学習」の前提となる **学習 data (policy_target=MCTS root visit 分布) を生成する exporter が欠落**していた gap を埋める。exp/059 の `train_value.py --policy` は npz key `policy_target` を読むが、`export_value_data.py` は value (MC outcome) しか書かず policy head の学習 data を生成不能 = H017 GPU escalation が現状 un-runnable。本 iter で (1) `mcts.py` の `search()` 本体を read-only `_search_root()` に抽出 (agent parity 完全保持) + `search_root_visit_policy()` 追加 (root visit を policy index 規約 = `_assign_child_priors` と 1:1 で集約・正規化)、(2) `export_value_data.py --policy-budget` で各 timestep の visit 分布を `policy_target` として npz 記録。**【exp/061 done — infra step + 重要発見】** mcts.py: `search()` 本体を read-only `_search_root()` に抽出 (agent parity 完全保持) + `search_root_visit_policy()` 追加。export_value_data.py: `--policy-budget` で policy_target を npz 記録 + `policy_noop_frac` 診断 stat。smoke (value+policy) / tiny e2e (policy_row_sum=1.0, policy_dim=17) pass、pytest 73 pass、ruff clean、agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/ src/strategy/` = 0)・no-submit infra_merge。**発見 (learned_rules `policy_index_truncates_own_planets`)**: policy index (ships 降順 top-16 slot) は launch 元 own planet を mid/late game で頻繁に truncate し policy_target が no-op に collapse (e2e `policy_noop_frac=0.67`)。**現規約のまま GPU 学習すると policy head は no-op 偏重で near-useless** → H017 winrate go/no-go 前に index 規約を own-planet 中心に修正 (encoder でなく policy 層を train/serve 1:1) が前提。次 step: local で index 規約修正 → Kaggle GPU dual-head 実学習 (visit 分布 target) → NN-in-MCTS winrate eval (supervisor gating)。実 data 生成 (scale) は `local_cpu_selfplay_export_infeasible_at_scale` で Kaggle escalate。in_progress 継続。 **exp/062 着手 (infra step — policy index 規約修正)**: exp/061 で発見した `policy_index_truncates_own_planets` (policy_noop_frac=0.67) を GPU を焼く前に local で修正。`src/features/encoder.py` に `select_planet_tokens` helper を追加し、planet 数>MAX_PLANETS の切り詰め時のみ **自軍 launch 元を優先保持**・残りを ships 降順非自軍で充填 (token 順序は全体 ships 降順を維持 = membership のみ変更)。encoder + MCTS serve 2 関数 (`_assign_child_priors` / `search_root_visit_policy`) が本 helper を共有し policy slot index を encoder token 位置と構造的に 1:1 化 (train/serve skew ゼロ)。**【exp/062 done — infra step】** encoder smoke で >MAX_PLANETS 局面の自軍全保持 + token 順序 ships 降順 + planet<=MAX_PLANETS の従来挙動不変を assert。value_infer skew=0 smoke 維持・PUCT prior spread 0.0465 維持・model dual ONNX round-trip (value 2.1e-7/policy 3.1e-7) 維持。pytest 73 pass・ruff clean・agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/ src/strategy/` = 0、提出 435.3 維持)・no-submit infra_merge (exp/055/059/060/061 先例)。NN architecture / POLICY_DIM / train_value.py / export_value_data.py は不変。**value path 影響**: planet>MAX_PLANETS 局面で c5 checkpoint の retained set が変わるが c5 は diagnostic-only (提出物外)・permutation-invariant pooling ゆえ <=MAX_PLANETS で完全不変・次 GPU run で新 encoding 再学習のため許容。**次 step (Kaggle GPU escalate)**: 修正済 index で実 self-play data の dual-head 学習 (visit 分布 target) → MCTS PUCT prior + NN value leaf で strong-opponent winrate eval (supervisor gating)。in_progress 継続。 **exp/065 着手 (infra step — Kaggle notebook を dual-head policy 配線)**: exp/055-064 で H017 の全 local infra (PolicyValueNet / exporter `--policy-budget` / train `--policy`+launch-row CE filter H032 / MCTS PUCT prior) を build 済だが、GPU escalation の実行体 `kaggle_notebooks/nn-value-train/nn_value_train.py` は value-only (H016) 配線のままで dual-head policy path を一切呼ばない = supervisor が現状 push しても value-only model しか出ず H017 が un-runnable。本 iter で notebook を `POLICY=1` opt-in の dual-head mode に拡張 (stage1 export に `--policy-budget`、stage2 train に `--policy`、value-only 既定は不変で H016 path 保護)。agent byte 不変・no-submit infra_merge。 **exp/066 着手 (in_progress — quantize 段の dual-head 検証 gap)**: supervisor GPU run 前の local de-risk として exp/055-065 chain (export→train→quantize→serve) を tiny synthetic data で end-to-end 実走し stitching を検証 → wiring バグ無し (int8 dual-head serve OK)、ただし **stage3 `quantize_onnx.py` の `evaluate()`/`_measure_latency()` が `sess.run(["value"],..)` 固定で `policy_logits` を一切検証しない gap を発見**: int8 量子化が PUCT prior を壊しても stage3 が success を返し supervisor の GPU run が壊れた prior を serve する risk。本 iter で quantize の検証を dual-head ONNX 出力 auto-detect に拡張し、policy_logits が量子化後も生存 (finite / softmax 総和 1 / padding slot prob ~0) を検証する step を追加。notebook 側は無改修 (CLI が出力名で自動分岐)。agent byte 不変・no-submit infra_merge。 **exp/067 done (infra step — GPU run quota 保護)**: exp/055-066 で H017 の全 local infra (model/exporter/train/quantize/serve) を build・de-risk 済だが、GPU 起動の最後の blocker `kaggle_p100_pytorch_incompat` (cycle1/exp046 で P100 sm_60 が data-gen 完了後の train 段で CUDA no-kernel-image error → ~2.5h quota 浪費) が未対処だった (kernel-metadata.json の GPU type pin は Kaggle CLI で不確実)。train_value.py に `_select_device()` を追加し、`torch.cuda.is_available()` (P100 でも True) を通過後に tiny tensor を CUDA 上で実演算 probe → 例外時 CPU 自動 fallback (value-only L190 / dual-head L436 の 2 call site 共有)。210K param/40epoch の CPU train は cycle1 で ~15min 実証済ゆえ P100 引当でも成果物が確実に出る。smoke 両 path pass・fallback 分岐 monkeypatch 検証・pytest 73 pass・ruff clean・agent byte-identical (`git diff main -- main.py src/search/beam.py src/agents/ src/strategy/ src/features/ src/nn/` = 0、提出 435.3 維持)・no-submit infra_merge。**これで H017 dual-head GPU run (POLICY=1) は GPU type を問わず supervisor が push&run 可能 = critical path の起動 blocker 解消**。次 step (supervisor GPU escalate): POLICY=1 で実 self-play dual-head 学習 → MCTS PUCT prior + NN value leaf で strong-opponent winrate eval (`nn_in_mcts_leaf_local_eval_infeasible_in_harness` ゆえ Kaggle 側 self-play eval)。 |
| H018 | done | 3 | 3 | 6 | +40 | 4-fold symmetry data augmentation (90/180/270° rotation で学習データ 4倍) | 学習収束が 1/4 epoch で同等 loss | PLAN.md B-5。**exp/052 着手**: NN critical path の data 効率改善 (data-gen は local infeasible/Kaggle escalate 済 = `local_cpu_selfplay_export_infeasible_at_scale`、実 game を増やさず encoded tensor の 90/180/270° 回転で train data 4x)。board 100×100 正方形・sun 中心 radial gravity ゆえ 90°刻み回転は exact symmetry (mirror は angular_velocity 符号を壊すため rotation のみ)。agent byte 不変 (main/beam は import せず)・no-submit infra_merge (H016 step 先例)。**【exp/052 done — augment infra 完成・機構検証済】** `src/features/symmetry.py` (整数演算で exact 90°刻み回転、encoder 列 index に厳密対応: planet xy=(3,4) / fleet xy=(2,3) dir=(4,5)、`_norm_xy` の中心原点正規化ゆえ原点中心回転で float drift ゼロ) + `train_value.py --augment` (回転は grouped split **後**の train_idx にのみ適用 → train/val leakage なし、val は honest 汎化測定のため非拡張)。pytest 73 pass、symmetry smoke OK (90°×4=identity / 180° sign-flip / 方向ノルム保存 / copy 安全)、train smoke で n_train 72→288 (4x)・val=24 非拡張・loss 0.93→0.00 収束・ONNX round-trip 5.96e-08 を assert。**実 data での「1/4 epoch で同等 loss」効果測定は Kaggle GPU train run 依存** (`local_cpu_selfplay_export_infeasible_at_scale`、supervisor gating)。agent byte-identical (git diff main -- main.py src/search/beam.py src/agents = 0、提出 435.3 維持)・no-submit infra_merge |
| H019 | blocked | 4 | 4 | 6 | +80 | 戦況分類 (序盤/中盤/終盤/危機) に基づく dynamic dispatch (NN/MCTS/heuristic 切替) | vs Phase 3 N=30 winrate≥55%、LB ≥1300 | PLAN.md L380-381。**【blocked-on-phase3】** dispatch 対象の NN/MCTS が baseline 超え未達 (H016 cycle 6 で MCTS+NN は local eval infeasible、NN-in-beam は neutral)。切替元となる viable な代替戦略が beam (noop) 1 本のみ = dispatch 不能。Phase 3 NN が baseline 超えしてから active 化 (priority-sort 誤選択を防ぐため active→blocked) |
| H020 | discarded | 4 | 3 | 5 | +50 | endgame all-in mode (残 1 敵かつ自軍 ship 数 > 敵×1.5 で全 fleet 集中派兵) | endgame での勝率 +20% | PLAN.md A-19。**【exp/040 discard】** beam.search() 冒頭に endgame override (生存敵=1人 & own_ships>enemy×1.5 で全 own planet→最寄り敵へ全 ship 派兵)。fresh mix-eval: random 0.9667(29) / **nearest_sniper 0.3667(11)** / **prev_best 0.5667(17)** / winrate_min 0.3667 / errors=0。真 noop baseline (0.9667/0.6/0.7) 比で sniper **-7g**・prev_best **-4g** の明確な regression、gate FAIL (winrate_min<0.50 + sniper no-regression 違反)。「mid-game に介入しない bounded late-game」想定が誤り: dominance 条件 (own>enemy×1.5) は mid-game で先行時に発火し、全 planet を空にして sniper の反攻に punish される over-commit。advisor 警告 (over-expansion 機構は opponent-model と独立で noop でも治らない、passive horizon 拡張で悪化しうる) が的中。beam.py を baseline に revert (agent bit-identical, 提出 435.3 維持)、no-submit。helps-weak/hurts-strong の 9 系統目 (eval×3/depth/opponent/fleet/paradigm/rollout/comet + endgame-allin) |
| H025 | done | 1 | 5 | 2 | **+21.1 LB (実証)** | beam の `_simulate_opponents` を **noop** 化した A/B 診断。**【2026-05-30 done】** exp029 (PR #60) は既に main merge 済、提出 53126719 の LB = **435.3 (+21.1 vs baseline 414.2)** で 4月以来初の実 LB 改善。提出時 local mix-eval は prev_best 0.4667 (gate FAIL 相当) だったが LB +21、本 iter 再測定では prev_best 0.7 (PASS) = N=30 mix-eval が同一コードで 0.23 (7試合) も振れる高分散を実証 (learned_rules `mixeval_prevbest_gate_noisy_misjudges_lb`)。新 baseline として best_score.json に fresh 値 (0.9667/0.6/0.7) で同期 (reconciliation iter)。探索内 opponent を静止扱いする方が phase1 rollout モデルより LB 上 優位 = beam の opponent model が真因という解釈を LB データで裏付け | LB 435.3 (>414.2) で go 確定 | learned_rules INFO; beam.py:486-490 `_simulate_opponents`=noop (現 main) |
| H027 | discarded | 1 | 5 | 1 | +0 (測定不能) | noop opponent baseline 上で SEARCH_DEPTH 2→3 を再検証 (depth×opponent matrix の空白セル)。**【exp/042 discard — gate FAIL かつ depth 効果が N=30 検出閾値以下】** fresh mix-eval: random 0.9(27) / nearest_sniper 0.6(18) / prev_best 0.5(15) / errors=0。official depth=2 baseline (0.9667/0.6/0.7 = 29/18/21) 比で gate FAIL (random -2g)。**しかし directional read は inconclusive**: この 27/18/15 は exp041 ledger の **同日 byte-identical depth=2 baseline 再測定 (27/18/15) と完全一致**し、depth=2 の run-to-run 変動レンジ (official 29/18/21 ↔ exp041 27/18/15) 内。tournament は timing-bounded search で run 間変動があり、N=30 では depth=3 と depth=2 を分離できない (`mixeval_prevbest_gate_noisy_misjudges_lb` 追認)。crater (≤14) でも hold (~20-21) でもなく「検出閾値以下」。beam.py を baseline に revert (git diff main -- src/ main.py = 0 行, agent bit-identical, 提出 435.3 維持)、no-submit | mix-eval N=30 (gate FAIL, advisory) | beam.py:19 SEARCH_DEPTH; learned_rules `depth3_noop_regime_below_n30_detection` (新規) / `deeper_search_overfits_wrong_opponent_model` / `opponent_model_not_root_cause_of_depth_regression` / `mixeval_prevbest_gate_noisy_misjudges_lb` |
| H028 | awaiting_supervisor | 1 | 5 | 1 | +? (LB lever 候補) | **【exp/043 PR #76 — awaiting_supervisor、re-litigate 禁止】** 実装・timing safety 完了済 (`_BEAM_TIME_BUDGET_SEC` 0.5、main.agent max 601ms<950ms、73 test pass)。mix-eval gate は **構造的に un-passable** (`check_mix_gate` は random≥baseline=0.9667=29/30 を要求するが `tournament_noise_from_wallclock_cutoff_under_parallel_load` の noise floor は ~27/30。byte-identical baseline の exp041/042 も 0.9 で gate FAIL)。よって branch B (gate PASS→submit) はどの agent でも到達不能で、H028 submit は supervisor の **deliberate gate-override** 判断 (exp029 と同型、+21 LB を出した先例) でしかありえない。autonomous loop は agent-modifying・LB-未検証 の変更を自前二層 gate を bypass して push しない (merge も submit もしない)。**beam time budget 過小利用の是正**: 提出 agent は actTimeout 1s (main.py deadline 0.90s) に対し `_BEAM_TIME_BUDGET_SEC=0.3` で beam を 0.3s に self-cap。本 iter の primary-source 診断で、dense/late turn では beam が 0.3s budget を恒常的に超過して打ち切られ (mirror probe seed3: median 308ms/max 589ms、267/300 turn が >240ms)、利用可能 budget の 1/3 しか使っていないことを実測。`_BEAM_TIME_BUDGET_SEC` を 0.3→**0.5** に上げ、検証済 beam (noop, LB 435.3) に pivotal turn でより深い探索時間を与える。9 系統 kill の「新 eval/depth/paradigm 追加」とは別カテゴリ (既存 beam に本来の compute を供給するだけ) | timing test <950ms を **hard safety gate** (DQ ゼロが絶対制約)。実測 (main.agent, seed7, 300turn): budget 0.5 で max 601ms (1000ms hard limit に ~400ms margin)、median は budget 不変 95ms。mix-eval no-regression gate は **advisory** (`mixeval_prevbest_gate_noisy_misjudges_lb`、N=30 は ±7試合振れる)。真の judge は LB | 本 iter 診断。`beam_budget_underutilizes_acttimeout` / `tournament_noise_from_wallclock_cutoff_under_parallel_load` (新規 learned_rules)。supervisor escalate: budget を 0.6+ に上げる / time-check granularity 細粒度化は Kaggle-side latency telemetry が必要 |
| H026 | discarded | 2 | 5 | 2 | +0 (診断) | MCTS rollout policy を `_phase1_decisions` → uniform random に置換した A/B 診断 (mcts.py `_rollout_value`)。exp028 の MCTS strong-opponent 激減 (sniper 0.20 / legacy 0.1667) が phase1 rollout policy 由来かを切り分ける。`ROLLOUT_POLICY=uniform` env で切替、main default (beam) parity 不変 | relaxed gate (random>=0.8667 AND sniper>=0.5667 AND winrate_min(random,sniper)>=0.50 AND errors=0) を `ORBIT_WARS_MCTS=1 ROLLOUT_POLICY=uniform` で測定し phase1 版 (exp028) と比較。MCTS は env OFF (提出物外) のため **no-submit 診断** (LB/quota 影響ゼロ) | learned_rules L61 推奨 (b); advisor 助言。mcts.py のみ改変。**exp/030 で discard (診断完了)**: uniform rollout で random 0.9667 / nearest_sniper 0.30 / prev_best 0.1333 / winrate_min 0.1333、relaxed gate FAIL (3 条件)。phase1 rollout (exp028: 1.0 / 0.20 / 0.1667 / winrate_min 0.1667) と比較し strong-opponent regression はほぼ不変 (sniper 0.20→0.30 僅改善 / legacy 0.1667→0.1333 僅悪化)。**rollout policy (phase1↔uniform) は MCTS strong regression の主因でない** = (a)(b)(c) の (b) 消込。真因は上流 (root-only 木 / sigmoid scale / eval / measurement)。learned_rules INFO `mcts_rollout_policy_not_root_cause` 昇格。mcts.py は ROLLOUT_POLICY env 残置 (default phase1、main parity 不変) |
| H030 | done | 1 | 4 | 1 | +0 (測定基盤) | **mix-eval gate 測定 infra 修正**: tournament.py に `--prev-best-n` per-opponent override を追加し、gate の唯一の decision軸 prev_best (legacy-388) のみ高 N (≥100) で測定可能に (random は 0.9667 天井 / sniper 0.6 で識別力低・N=30 で足りる、prev_best のみ同一コードで±7試合振れ gate を両方向に誤判定 = `mixeval_prevbest_gate_noisy_misjudges_lb` / `mixeval_prevbest_gate_rejected_lb_winner` が唯一の +21 勝者 exp029 を reject していた)。default 未指定=従来挙動 (compat 不変)。current baseline で N=100 を 1 回回し安定 prev_best baseline 値を確立。**discipline (gate hard/advisory・auto-submit) は不変** (supervisor 領分、advisor 助言)。agent-invariant (main.py/src byte 不変)・no-submit・infra_merge | agent byte 不変のため gate は advisory、deliverable は安定 baseline 値の確立 (H021/H016 infra_merge 先例) | advisor 助言 (2026-05-31); learned_rules `mixeval_prevbest_gate_noisy_misjudges_lb` 防止策「gate winrate は最低 N=100」/ `fleet_projection_freeze_not_root_cause` 防止策(b)。**【exp/045 done — N=100 baseline 確立】** `--prev-best-n 100` 実装 (tournament.py +22/-1, agent byte 不変、pytest 73 pass) → 現 main で N=100 計測: random 28/30 (0.9333) / sniper 18/30 (0.6) / **prev_best 51/100 (0.51)**。**決定的: prev_best 真値 ≈ 0.51 (ほぼコインフリップ)**、N=30 で振れた 0.4667↔0.7 は両側とも真値から大きく外れた noise = legacy-388 mirror gate 軸は現 agent 周辺で識別力ほぼゼロを確定。N=100 でも σ≈0.05 で ±1σ 改善 (0.56) 検出も限界 → go/no-go は LB delta 単独が妥当。安定値は best_score.json `prev_best_n100_baseline` に記録。gate advisory (agent 不変、no-submit, infra_merge)。learned_rules `mixeval_prevbest_gate_noisy_misjudges_lb` を N=100=0.51 で補強。**headline escalation: local knob 空間は LB 確定で枯渇 (単一 knob 10 系統 kill)、escape は NN GPU run のみ = critical path** |
| H029 | discarded | 1 | 4 | 2 | +0 (診断: 改善なし) | **【exp/044 discard】anytime-beam 診断**: beam.search() の hard-timeout 分岐 (beam.py:544-545 / 555-556) は `best_frontier` (完了済 depth の完全 beam) を保持しているのに **破棄して phase1 greedy fallback に落ちる**。`beam_budget_underutilizes_acttimeout` の実測で dense/late turn (267/300) はこの timeout 経路が hot。timeout 時に best_frontier の `_score_state` argmax を返す anytime 化に変更し、phase1 fallback が 435.3 の load-bearing 要素か (= dense state で beam を信頼すると prev_best で crater するか) を測る。**advisor 警告: bounded downside でない** — timeout は opponent-model mismatch が最も効く最難 state で発火、helps-weak/hurts-strong がここで噛む両側リスク。H028 (beam compute 増) の de-risk に直結 | 判別は **vs prev_best の符号** (N=30 は ±7試合で判別不能、prev_best directional read を deliverable に)。gate は構造的 FAIL 織込済 → no-submit 診断、crater なら revert + discard | beam.py:544/555 hard-timeout 分岐。H007 (eval-term grid、`new_eval_term_helps_random_hurts_strong` で pre-killed) を skip。advisor 助言。**結果 (matched A/B, prev_best のみ N=60)**: anytime 34/60 (0.5667) vs baseline 38/60 (0.6333) = **-4g (-0.0667 ≈ 1σ未満、判別閾値以下)**。N=30 初回は anytime 13/30 (0.4333) と低めに振れたが N=60 で 0.5667 に回帰 = `mixeval_prevbest_gate_noisy_misjudges_lb` 再現。**符号は強相手 prev_best で非改善・僅か下方**: dense/late turn で phase1 fallback を完全 beam に置換しても strong opponent で得しない (advisor 警告どおり helps-weak/hurts-strong の縁)。phase1 fallback は drag でなく load-bearing 寄り。go 基準 (prev_best 改善) 不達 + 構造的 gate FAIL → no-submit、agent baseline revert (bit-identical, 435.3 維持)。**H028 (beam compute 増) の de-risk**: beam に dense-turn の影響を増やす方向は strong-opponent upside を生まない可能性が高いと示唆。learned_rules `anytime_beam_no_gain_vs_strong` 新規 |
| H032 | done | 3 | 4 | 1 | +0 (NN de-risk: GPU policy 学習を有効化) | **【exp/064 done — infra step】** `_epoch_pv` に `policy_launch_only` (既定 True) を追加し policy CE を launch row のみで計算 (死 row 除外、value MSE は全 row 維持)、smoke で死 row 半数注入 → launch_frac 0.5 vs all-rows 1.0、launch row CE 2.16→0.68 < uniform 上限 2.83 = collapse 回避を実証。`policy_launch_frac` stat 報告、`--policy-all-rows` で diagnostic 比較可。pytest 73 pass・ruff clean・agent byte-identical (提出 435.3 維持)・no-submit infra_merge。次 step (Kaggle GPU escalate): 修正済 train path で dual-head 実学習 → MCTS PUCT prior + NN value leaf で strong-opponent winrate 測定 (supervisor gating)。**【exp/064 着手 — train_value.py policy CE を launch 決定 timestep に restrict】** H031 (exp/063) の結論「現状の policy_target をそのまま GPU dual-head 学習に渡すと CE loss が ~70% の trivial no-op 死 row に支配され policy head が no-op collapse → **GPU escalate 前に train_value.py で policy_target を launch 決定 timestep に filter/weight する修正が前提**」を実装。`_epoch_pv` の policy cross-entropy を **launch row (exporter 定義 `policy_target[:,:MAX_PLANETS].sum > 1e-6` = MCTS が launch を 1 度でも visit) のみ**に restrict (死 row は CE から除外、value MSE は全 row 維持 = MC outcome は全 timestep valid)。default ON (GPU 学習が必ず filter を通す)、`policy_launch_frac`/`n_policy_rows` を stats 報告し GPU run が data 品質を honest 監視。agent byte 不変・no-submit infra_merge | smoke: 死 row 注入 dataset で CE が launch row のみで計算され収束 + 全 row では no-op collapse することを assert。実 dual-head 学習/winrate eval は Kaggle GPU escalate (`local_cpu_selfplay_export_infeasible_at_scale`) | exp/063 `policy_target_noop_collapse_persists_after_index_fix` の防止策。exporter `policy_launch_decision_frac` (exp/063) と同一 launch-row 定義で train/serve 整合。H017 critical path の直接継続 |
| H033 | done | 3 | 4 | 2 | +0 (NN go/no-go signal を Kaggle で取得可能化) | **【exp/068 done — infra step】** `nn_value_train.py` に `EVAL_RUN=1` opt-in の stage 4 を追加 (既定 OFF = 従来 train→quantize で完全不変)。trained int8 を `ORBIT_WARS_MCTS=1 ORBIT_WARS_NN_VALUE=1`(+POLICY 時 `ORBIT_WARS_NN_POLICY=1`) で wire し `tournament.py --agent main.py --opponents random,nearest_sniper,prev_best --n-per-opponent EVAL_N --max-workers EVAL_WORKERS(既定 2、local OOM=高並列メモリ圧を回避)` を回し `nn_mcts_winrate.json` を出力。`EVAL_MODEL` で dataset mount 済 model override 可。検証: py_compile OK / tournament CLI 引数確認 / pytest 73 pass / ruff clean / agent byte-identical (`git diff main -- main.py src/ scripts/` = 0、提出 435.3 維持)・no-submit infra_merge。**これで supervisor は POLICY=1 EVAL_RUN=1 で push&run すれば 1 run で dual-head 学習 + go/no-go winrate signal が出る = H016/H017 critical path 最後の Kaggle-side gap を closure**。go/no-go: strong opponent (sniper 0.6 / prev_best 0.51) が baseline 付近に戻り MCTS+NN cycle1 (sniper 0.20/prev 0.17) から回復するか。 **exp/068 着手 (infra step — Kaggle 側 NN-in-MCTS winrate stage)**: H016/H017 の全 local infra (model/exporter/train/quantize/serve/notebook) は exp/055-067 で build 済だが、trained dual-head model の **go/no-go signal = NN-in-MCTS (PUCT prior + NN value leaf) の strong-opponent winrate** は `nn_in_mcts_leaf_local_eval_infeasible_in_harness` (local harness が 8-worker 並列下で OOM/reap、~466s で親死亡) で **ローカル測定不能 → "Kaggle 側 self-play eval" に escalate 済みだが、その eval infra が未実装** (`nn_value_train.py` は train→quantize で終わり L213-219 が次手を手投げ)。supervisor が GPU run で model を得ても winrate signal が取れず critical path が stuck。本 iter で notebook に `EVAL=1` opt-in の stage 4 を追加: trained int8 model を `ORBIT_WARS_MCTS=1 ORBIT_WARS_NN_VALUE=1`(+POLICY 時 `ORBIT_WARS_NN_POLICY=1`) で wire し `tournament.py --agent main.py --opponents random,nearest_sniper,prev_best` を **低 worker** (local OOM 回避) で回し winrate JSON を出力。agent byte 不変・no-submit infra_merge | smoke: notebook の python compile + tournament.py CLI が NN env 付きで起動すること。実 winrate 測定は Kaggle GPU run (supervisor gating)。go/no-go は strong opponent (sniper/prev_best) が baseline 0.6/0.51 付近に戻るか | `nn_in_mcts_leaf_local_eval_infeasible_in_harness` (eval の Kaggle escalate) / `local_cpu_selfplay_export_infeasible_at_scale` (data-gen 版の先例) / exp/065 notebook 配線。H017 critical path の直接継続 |
| H031 | done | 3 | 4 | 1 | +0 (NN de-risk: GPU quota 節約) | **【exp/063 done — informative-negative】** real data 2 seed で policy_noop_frac 0.762/0.714 = exp/061 の 0.67 と同等以上で **未改善**。真因は encoder truncation でなく noop-beam の game-flow sparsity (strided timestep の 64-74% が launch 決定なし=死 row)。**ただし launch 決定 timestep のみでは noop 0.10-0.19 で policy head は学習可能**。GPU dual-head 学習 (quota 消費) 前に train_value.py で policy_target を launch 決定 timestep に filter/weight する修正が前提と確定 → 次 hypothesis (H032 候補)。exporter に policy_launch_decision_frac/policy_noop_frac_launch_only 診断 stat 追加。agent byte-identical・no-submit。learned_rules `policy_target_noop_collapse_persists_after_index_fix`。 **exp/063 着手 (診断 — GPU escalate 前の real-data 検証)**: exp/062 の policy index 修正 (`select_planet_tokens`: planet 数>MAX_PLANETS の切り詰めで自軍 launch 元を優先保持) は **synthetic encoder smoke のみで検証**され、exp/061 が real e2e で観測した `policy_noop_frac=0.67` (自軍 truncation で policy_target が no-op collapse) が**実 self-play data で解消したか未確認**。GPU dual-head 学習 (quota 消費・supervisor gating) を起動する前に、small real export (`export_value_data.py --policy-budget`、修正済 index を通る serve 経路) で policy_noop_frac を実測し exp/061 baseline 0.67 と比較する de-risk。agent byte 不変・no-submit | go = real game で noop_frac が大幅低下 (>MAX_PLANETS 局面が実在し fix が効く → GPU 学習 green light) / informative-negative = 実 game は ≤MAX_PLANETS で truncation 自体が稀 (fix は冗長だが無害、policy collapse の真因は別) | exp/061 `policy_index_truncates_own_planets` / exp/062 index 修正。`local_cpu_selfplay_export_infeasible_at_scale` ゆえ scale data は Kaggle、本 iter は signal 用 small N のみ |

| H034 | done | 3 | 4 | 1 | +0 (NN de-risk: GPU quota 保護) | **【exp/069 done — infra step】** `scripts/nn/smoke_eval_stage.py` 追加: tiny dual-head int8 (`_export_dualhead_synthetic`+`quantize_int8`) を NN-in-MCTS path (`ORBIT_WARS_MCTS=1`+`NN_VALUE`+`NN_POLICY`) に親プロセス env 経由で通し、最小 scale (random/N=1/workers=1) で tournament→kaggle `env.run`→main.py→mcts→`nn_mcts_winrate.json` seam を **end-to-end 実走** (~2:10、winrate=1.0 wins=1/1 **errors=0** = NN model が valid に load され MCTS が valid moves を返した)。これで supervisor の single GPU run (POLICY=1 EVAL_RUN=1) stage-4 が valid な winrate signal を出すことを GPU を焼く前に local で保証 = exp/068 が py_compile+CLI 確認のみで残した検証 gap を閉じる。pytest 73 pass・ruff clean。agent byte-identical (`git diff main -- main.py src/` = 0、提出 435.3 維持)・no-submit infra_merge。mix-eval gate は **BLOCK だが byte-identical noise** (random 0.9333=-1g < 0.9667、`tournament_noise_from_wallclock_cutoff_under_parallel_load` の noise floor、agent 不変ゆえ真の regression でない)。次 step: supervisor が POLICY=1 EVAL_RUN=1 で GPU run → dual-head 実学習 + NN-in-MCTS winrate go/no-go signal。〜以下着手時メモ〜 exp/068 (H033) で notebook に追加した stage-4 (`EVAL_RUN=1` NN-in-MCTS winrate) は **py_compile + CLI 引数確認のみ**で、tiny model で end-to-end 実走して `nn_mcts_winrate.json` を生成する smoke が未実施 = exp/066 が stage1-3 に対して閉じた検証ギャップの stage-4 版。残る未検証 seam: tournament subprocess (→ kaggle `env.run` → main.py → mcts、NN env を親プロセス os.environ に set) が完走し winrate JSON を生成するか。これが壊れていると supervisor の single GPU run (POLICY=1 EVAL_RUN=1) が model を得ても winrate signal が出ず quota 浪費 + round-trip。本 iter で `scripts/nn/smoke_eval_stage.py` を追加し tiny dual-head int8 (`_export_dualhead_synthetic`+`quantize_int8`) を NN-in-MCTS path に通し最小 scale (random / N=1 / workers=1) で winrate JSON 生成を assert。agent byte 不変 (MCTS env OFF default、提出 435.3 維持)・no-submit infra_merge | smoke が stage-4 invocation を最小 scale で完走し JSON の期待 key (mode/opponents/winrate_min/errors_total) を assert。実 winrate 測定は Kaggle GPU run (supervisor gating) | exp/068 stage-4 配線 / exp/066 (stage1-3 de-risk 先例) / `nn_in_mcts_leaf_local_eval_infeasible_in_harness` (full eval は Kaggle escalate、本 smoke は wiring 確認の最小 1 game のみ)。H017 critical path の直接継続 |

| H035 | done | 3 | 4 | 2 | +0 (NN go/no-go signal を local 取得可能化) | **【exp/070 done — infra step】** `scripts/selfplay/winrate_runner.py` 追加: match.py を逐次 (--workers 既定 1) 起動し 1 ゲームごとに JSONL 追記・resumable (既出 game_id skip)、NN env を子 subprocess へ継承。**smoke (NN env 付き 1 ゲーム vs random)**: agent_won=true / 435 steps フルゲーム完走 / errors=0 = NN-in-MCTS path (c5 int8 value leaf) が valid load され MCTS が valid moves を返す end-to-end 確認。per-game 127s。resume 検証: 完了 game_id を skip し pending のみ実行・集計 honest を assert。`nn_in_mcts_leaf_local_eval_infeasible_in_harness` の真因 (8-worker 並列 OOM + 親が全完了まで結果保持で途中ロスト) を逐次+逐次追記で解消 → exp/050 で local 測定不能だった NN-in-MCTS strong-opponent winrate を GPU 不要で accumulate 可能化。pytest 73 pass・ruff clean・agent byte-identical (`git diff main -- main.py src/` = 0、提出 435.3 維持)・no-submit infra_merge。mix-eval gate は BLOCK だが byte-identical noise (random 0.9333<0.9667、`tournament_noise_from_wallclock_cutoff_under_parallel_load`、sniper 0.6=baseline PASS / prev_best 0.5=N100 真値)。**次 step**: strong-opponent eval (prev_best,nearest_sniper N=20) を accumulate (resumable) → go/no-go (MCTS+NN value が sniper 0.6/prev_best 0.51 付近に回復し plain MCTS 0.20/0.17 から改善するか) を local 判定。〜以下着手メモ〜 **NN-in-MCTS の go/no-go winrate signal を GPU 不要でローカル取得する resumable runner**。`nn_in_mcts_leaf_local_eval_infeasible_in_harness` (exp/050) の真因は agent 実行時間でなく tournament.py ProcessPool が NN ロード subprocess を 8 並列起動して OOM/reap し、かつ親が全完了まで結果を保持(途中死亡で全ロスト)すること。match.py は既に 1 ゲーム=1 subprocess のクリーン単位 → `scripts/selfplay/eval_resumable.py` を追加: **逐次 (workers=1 既定) で 1 ゲームごとに JSONL へ追記・resumable** (既出 seed は skip)、NN env (`ORBIT_WARS_MCTS/NN_VALUE/MODEL`) を子 subprocess へ継承。これで OOM を回避し、c5 value model (distribution shift 解消済) で **MCTS+NN value leaf の strong-opponent winrate** (H016 cycle 1 以来 blocked のコア signal) をローカルで accumulate。GPU 不要・Kaggle escalate を待たずに go/no-go (sniper 0.6 / prev_best 0.51 付近に回復するか) を測れる | agent byte 不変 (env-gated、提出 435.3 維持)・no-submit infra_merge (H016 step 先例)。deliverable = runner + 1-game smoke (JSONL 生成 + NN load 確認) + 実 eval kickoff。go/no-go winrate 値は accumulate 後に判定 | `nn_in_mcts_leaf_local_eval_infeasible_in_harness` (本 rule をエンジニアリングで解消)、`nn_value_distribution_shift_legacy388_mirror` (c5 merged model)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N は strong-opponent 識別に必要分)。H016 critical path の直接継続 |

| H036 | done | 3 | 4 | 3 | +0 (NN go/no-go signal の実取得 = **GO**) | **【exp/071 done — measurement run, core signal 取得 = GO】** H035 runner を実走 (N=12/相手・errors=0・per-game 75-288s, agent byte 不変・no-submit)。**結果: nearest_sniper 9/12 = 0.75 / prev_best 5/12 = 0.4167 (winrate_min 0.4167)**。3 reference に対する位置: plain MCTS (exp028: sniper 0.20/prev 0.17) → MCTS+NN cycle1 (0.47/0.33) → **本 NN-in-MCTS (c5 int8, 0.75/0.42)** vs beam baseline (0.6/0.51)。**判定 GO**: (1) NN value leaf は MCTS の strong-opponent regression を**治す** = H016 が cycle1 以来 blocked だった core 質問に YES。(2) **sniper 0.75 は beam baseline 0.6 を超過** (plain MCTS 0.20 から +0.55、cycle1 0.47 から +0.28)。(3) prev_best 0.4167 は plain MCTS 0.17 から大幅回復 (>2σ) し baseline 0.51 付近 (N=12 σ≈0.14 で ~0.7σ 内 = 区別不能)、cycle1 0.33 より上。`nn_value_leaf_heals_mcts_strong_opponent_regression` 昇格。seat balance: prev_best A 2W/4L・B 3W/3L (severe bias なし)。**次手 (H016 next cycle / 新 hypothesis)**: (a) prev_best を N≥30 で蓄積し 0.42 vs 0.51 の gap を絞る (resumable runner で GPU 不要)、(b) NN-in-MCTS を beam baseline 435.3 と LB で答え合わせする経路を検討 (per-turn 255ms<900ms は exp/050 で submission-safe 確認済 = latency 障壁は無い)。〜以下着手メモ〜 **【exp/071 着手 — measurement run, infra theater 脱出】** H035 (exp/070) で build した resumable runner (`scripts/selfplay/winrate_runner.py`) を**実際に回し**、H016 が cycle 1 以来 blocked だった core signal = **NN-in-MCTS (c5 int8 value leaf, distribution shift 解消済) の strong-opponent winrate** を GPU 不要で local 取得する。exp/055-070 の ~15 iter は全て agent-invariant infra step (model/exporter/train/quantize/serve/notebook/runner) で、`local_cpu_selfplay_export_infeasible_at_scale` 系の壁を都度 escalate してきたが、runner 完成で「NN value が MCTS の strong-opponent regression (plain MCTS exp028: sniper 0.20/prev_best 0.17) を治すか」を**ローカルで答え合わせ可能**になった。本 iter は src 改変ゼロ (runner 既存)・新規 code なし、deliverable = **実 winrate 値 + go/no-go 判定**。`ORBIT_WARS_MCTS=1 NN_VALUE=1 MODEL=value_net_c5.int8.onnx` で prev_best+nearest_sniper を resumable accumulate (JSONL 永続・途中死亡耐性)。go = MCTS+NN value が baseline (sniper 0.6 / prev_best 0.51) 付近に回復 → NN leaf 路線継続 (H016 next cycle)。no-go = plain MCTS (0.20/0.17) 付近に留まる or cycle1 (0.47/0.33) 止まりで baseline 未達 → NN value leaf も beam 435.3 を超えず, escape は H017 policy prior (PUCT) or 強 teacher data へ。 | strong-opponent winrate (prev_best=gate/LB 軸 N100 真値 0.51 / nearest_sniper 0.6) を N≥10/相手 で accumulate。判定は plain MCTS 0.20/0.17・MCTS+NN cycle1 0.47/0.33・beam baseline 0.6/0.51 の 3 reference に対する位置で directional read (N=10-12 で 0.17↔0.47 の gap は分離可能、`mixeval_prevbest_gate_noisy_misjudges_lb` ゆえ 1-2g 差は判定に使わない)。agent byte 不変・no-submit infra_merge | `nn_in_mcts_leaf_local_eval_infeasible_in_harness` (H035 runner で解消した本 rule をついに行使)、`nn_value_distribution_shift_legacy388_mirror` (c5 merged model = legacy-388 戦 58%)、H016 cycle 6 / H035 「次 step」の直接実行。H016 critical path の core signal 取得 |
| H037 | done | 3 | 4 | 2 | +0 (NN go/no-go signal の統計強化 = 治すが超えない) | **【exp/072 done — measurement run, N=30 で「治すが超えない」確定 + H036 sniper 0.75 は noise と訂正】** H035 runner を N=30/相手まで蓄積 (既存 12 game skip・errors=0・c5 int8 value leaf, agent byte 不変・no-submit)。**結果: nearest_sniper 15/30 = 0.50 / prev_best 14/30 = 0.4667 (winrate_min 0.4667, σ≈0.09)**。**核心訂正**: H036 GO 根拠「sniper 0.75 が baseline 0.6 超過」は N=12 上振れ noise (N=30 で 0.50 に回帰、baseline 0.6 を ~1.1σ 下回る = `mixeval_prevbest_gate_noisy_misjudges_lb` の小N overread 実証)。prev_best は安定 (N=12 0.4167→N=30 0.4667、両 N とも baseline 0.51 と区別不能 -0.5σ)。**tempered 判定 (interpretation ii)**: NN value leaf は plain-MCTS strong-opponent regression (0.17/0.20→0.47/0.50) を**治す (頑健 >2-3σ)** が beam baseline (0.6/0.51) を**超えない**。NN value 単独路線は baseline tie 止まり → escape は **policy prior (H017, PUCT で value-tie を割る)** or **強 teacher data** or route (b) **LB 答え合わせ** (255ms<900ms submission-safe)。local N をこれ以上積んでも 0.47↔0.51 の ~0.5σ gap は識別実用外。learned_rules `nn_value_leaf_heals_mcts_strong_opponent_regression` に N=30 追補。 〜以下着手メモ〜 H036 (exp/071) の next手 (a) を実行。NN-in-MCTS (c5 int8 value leaf) の core signal は GO だが、gate/LB 軸 **prev_best が N=12 で 0.4167 (5/12, σ≈0.14)** = baseline 0.51 と区別不能 (~0.7σ 内)。「治る (plain MCTS 0.17 から大幅回復) が baseline を超えるか同等か」が N=12 では確定できない。H035 resumable runner で **prev_best+nearest_sniper を N=30/相手 まで蓄積** (既存 12 games は game_id skip、新規 18 games/相手)、σ を 0.14→0.09 に絞り 0.42↔0.51 の位置を確定する。GPU 不要・JSONL 永続・src 改変ゼロ (runner 既存)・agent byte 不変・no-submit measurement run (exp/071 先例)。go/no-go: prev_best N=30 が (i) 0.51 付近に上昇 → NN leaf が baseline 同等以上で LB 答え合わせ経路 (H016 next cycle / supervisor) を強く後押し / (ii) 0.42 付近に留まる → NN leaf は strong-opponent regression を治すが prev_best で baseline 未達、policy prior (H017) or 強 teacher data が次の lever。 | strong-opponent winrate を **N=30/相手** で accumulate (σ≈0.09)。判定は plain MCTS 0.20/0.17・MCTS+NN cycle1 0.47/0.33・beam baseline 0.6/0.51 の 3 reference に対する位置で確定。`mixeval_prevbest_gate_noisy_misjudges_lb` ゆえ 1-2g 差は判定に使わず CI 重なりで読む。agent byte 不変・no-submit infra_merge | H036 (exp/071) 次手 (a) の直接実行、`nn_in_mcts_leaf_local_eval_infeasible_in_harness` (H035 runner で解消)、`nn_value_distribution_shift_legacy388_mirror` (c5 merged model)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N=30 で σ を絞る)。H016 critical path core signal の統計強化 |

| H038 | in_progress | 3 | 4 | 2 | +0 (route (b) LB 答え合わせを supervisor-ready 化) | **【exp/073 着手 — route (b) NN-in-MCTS 提出 bundle de-risk】** H037 (exp/072) 確定次手 (ii)「route (b) LB 答え合わせ (per-turn 255ms<900ms submission-safe、local eval が両方向 noisy ゆえ LB が唯一の真 judge)」を supervisor が 1 提出で実行できるよう de-risk する。**現状 blocker**: `submit.sh` は main.py+src/ のみ pack し ONNX モデル/onnxruntime を bundle せず、main.py は NN-in-MCTS を env var (`ORBIT_WARS_MCTS=1` 等) でしか有効化できないが Kaggle 提出に env を渡す手段が無い = route (b) は提出 bundle が存在せず実行不能。本 iter で (1) `src/agents/nn_submission.py` (NN-in-MCTS agent factory、downside-bounded: onnxruntime/model 不在時は beam 435.3 に graceful fallback、NN 例外時も per-turn beam fallback)、(2) `scripts/kaggle/build_nn_submission.py` (c5 int8 onnx + src/ + bundle 専用 main.py を組み立て、kaggle_environments env.run で 1 game 実走し errors=0 + max_turn_ms<900 を assert) を追加。**live main.py / submit.sh は byte 不変** (本 module を import せず、提出物 beam = LB 435.3 維持)・no-submit infra_merge。残存 unknown = Kaggle 実機の onnxruntime 有無 (local 検証不能、graceful fallback で「不在なら 435.3 再現」と downside bound) は supervisor が route (b) 提出 1 回で答え合わせ。 | bundle が kaggle_environments env.run で errors=0 完走 + per-turn max<900ms。NN unavailable 時 beam fallback の pytest。go/no-go winrate でなく **bundle 実走 + downside bound** が deliverable (exp/067 が GPU run を supervisor-ready 化したのと同型)。agent byte 不変・no-submit infra_merge | H037 (exp/072) 次手 (ii) の直接実行、`nn_value_leaf_heals_mcts_strong_opponent_regression` (治すが超えない→LB が真 judge)、`mixeval_prevbest_gate_noisy_misjudges_lb` (gate advisory、LB delta 単独)、`nn_in_mcts_leaf_local_eval_infeasible_in_harness` (per-turn 255ms<900ms submission-safe)。H016 critical path の route (b) 経路を supervisor-ready 化 |

## In progress

(なし)

## Done

(なし)

## Discarded / Stuck

(なし)
