# Learned Rules

`analyze-deviations.sh` hook + `score-analyzer` subagent が同一エラー 3 回検出時にここへ昇格する (PLAN.md 自己改善ループ-1)。

`load-state.sh` hook が毎 session 開始時にこのファイルを context に注入し、`implementer` の system prompt に "次の rules を遵守: ..." として埋め込む。

## スキーマ
- `AVOID: <signature> — <root_cause>` 形式で 1 行 1 ルール
- `signature` は score-analyzer の `recurring_error_signature` 出力に対応

## ルール

- AVOID: `phase1_baseline_port_parity_skip` — src/ に移植した beam (legacy-388 と同 eval) が legacy-388 と N=30 mirror で winrate ≈ 50% に届かない状態で Phase 1 hypothesis (territory / projection / threat 等) を載せても、効果が parity gap に埋もれて誤判定される。Phase 1 着手前に必ず parity check (territory_weight=0 で legacy-388 vs new main.py が N=30 で 0.40-0.60) を通す。
  - 検出契機: exp/001-territory-eval で territory_weight=0/0.3 共に winrate 16.7%、seat swap でも 16.7%。
  - 根本原因 (未特定、要 H000 的調査): main.py wrapper の per-turn `_safe_act` pre-call、`sanitize` の二重実行、numpy lazy import overhead、または budget 計算式の差。
- AVOID: `mirror_match_same_path` — kaggle_environments の `env.run([a, a])` で同一 file path を渡すと 2 step で異常終了 (rewards=[0,0])。tournament.py が winner=0 をデフォルト集計して誤った 100% winrate を返す。mirror seat asymmetry を測る場合は別名 (e.g. `main_b.py`) のコピーを agent2 に指定する。
- AVOID: `supervisor_shared_working_tree` — human supervisor (or 別 claude session) が autonomous tmux session と同じ `~/Projects/orbit-wars/` working tree で git checkout/branch 操作すると autonomous claude も同じ branch を見てしまい状態混乱が起きる。supervisor は必ず `bash scripts/orchestrator/setup_supervisor_worktree.sh` で作成した `~/Projects/orbit-wars-watch/` 等の別 worktree で作業する。
  - 検出契機: 2026-05-16 iter 1 で supervisor が fix/guard-hook-executable ブランチを切った際、autonomous claude の git status が同 branch に切り替わり「ユーザーが切り替えた様子」を検知して state リセット動作を強いられた。
  - 防止策: CLAUDE.md「Intent: supervisor と autonomous loop の working tree 分離」+ scripts/orchestrator/setup_supervisor_worktree.sh で恒久化。
- AVOID: `hook_executable_bit_drift` — `.claude/hooks/*.sh` 新規追加時に `chmod +x` が抜けると git mode 100644 で commit され、autonomous loop で `Permission denied` の non-blocking hook error が継続発生する (deny rule のみが守りに残り、guard hook ロジックは機能不全)。
  - 検出契機: PR #8 (guard-dangerous-bash.sh) で chmod 抜け、iter 1 autonomous loop 走行中に Permission denied error が連発、後追い PR #24 で git update-index --chmod=+x 修正。
  - 防止策: smoke.sh に hooks の disk exec bit + git mode 100755 検証 check を追加 (今後の新規 hook で同じ抜けが起きたら Phase 0 smoke で fail する)。
- AVOID: `mirror_match_shared_src_import` — file 名を分けても (`main_bare.py` と `main_bare_b.py`)、両方が `from src.* import` で同じ module を共有すると同様に 2 step で異常終了する (rewards=[0,0])。`mirror_match_same_path` を file 名分けで回避するだけでは不十分。回避策: relative + try/except fallback の import (legacy-388 方式) で別 module key として load する、または subprocess 完全分離。検出契機: exp/000-parity-fix で確認。
- INFO: `port_beam_search_works_in_isolation_but_not_in_env_run` — 直接呼び出し (`agent(obs)`) では移植 beam (`main_bare`) と legacy-388 が **完全に同じ moves** を返す (確認: planets=[[0,0,30,30,3,100,2],[1,1,70,70,3,10,2]] obs で両者 `[[0, 0.5759..., 11]]`)。しかし `env.run([main_bare, legacy])` 経由では main_bare winrate 16.7% (N=30、seat swap でも同値)。env 内で 1) `from src.* import` を共有して agent が壊れる、2) legacy が `..search.beam` (relative) → `search.beam` (absolute fallback) で別 module としてロード → kaggle env 内の何かで legacy が有利な module 解決をする、のいずれか。次イテレーションは agent2 の import を legacy 方式 (relative+fallback) に揃えて再測定するのが H000 解決の入口。
- INFO: `main_bare_agent_not_invoked_in_env_run` — exp/000b 検証 (2026-05-17): main_bare の `agent()` 関数に `/tmp/main_bare_dump.log` への file write を仕込んで `env.run([main_bare, legacy], seed=1)` を実行 → 145 秒の試合は走るが log file が一度も作成されない。一方 `python -c 'from main_bare import agent; agent(obs)'` の直接呼び出しでは file が即時作成される。**kaggle_environments の orbit_wars 環境が main_bare の `agent` 関数を呼んでいない可能性** が浮上。H000 の真犯人候補は前回仮説の「import 共有」より更に上流の「agent loader の関数解決」。次イテレーション入口は kaggle_environments.envs.orbit_wars の source 確認 (agent 関数名は `agent` で正しいか、 callable / module path / file path のどれを期待するか、 file path の場合の load 方式)。
- AVOID: `agent_uses_dunder_file_at_module_top` — `__file__` を module top-level で参照する agent (`_ROOT = Path(__file__).resolve().parent` 等) は kaggle_environments の `get_last_callable` 経由で **NameError で crash**する。 agent.py L47 が `compile(raw, path_str, "exec")` + `exec(code_object, env)` を `env={}` で呼ぶため、`__file__` が env に注入されない。 crash 時は `get_last_callable` が `InvalidArgument` を raise し、agent が一切呼ばれない (fallback / no-op agent に置き換わるなど) ため対戦は形だけ進み、相手が確実に勝つ。
  - 検出契機: 2026-05-17 supervisor 調査で kaggle_environments/agent.py 読解 + main.py / main_bare.py を実 exec して `NameError: name '__file__' is not defined` を再現、修正後 main.py vs legacy-388 N=5 で winrate 0.167 → 0.40 (parity 期待値レンジ)。
  - 防止策: agent module top-level で `try: _ROOT = Path(__file__).resolve().parent; except NameError: pass` の guard を必ず入れる。kaggle agent loader は exec_dir を sys.path に append 済 (agent.py L53) なので fallback で何もしないで OK。
  - 関連: legacy-388 は `try: from .search.beam ...; except (ImportError, KeyError): ...` の構造で同じ問題 (`__name__` not in globals での relative import 失敗) を回避していた。今回の 修正で main.py + main_bare.py を同等以上に robust 化。
- INFO: `single_opponent_winrate_not_lb_predictive` — 単一相手 (legacy-388 のみ) のローカル N=30 winrate は LB を予測しない。exp 002 (H001 territory `TERRITORY_WEIGHT=0.3`) が両 seat 平均 63.3% (p1=73.3%, swap=53.3%) でも LB publicScore は baseline 比 **-17.3** (410.7→393.4) で discard。LB は random / nearest_sniper / 他参加者 submission の **mix 分布** に対する性能を測るため、単一相手では盲点が埋もれる。
  - 検出契機: 2026-05-21 supervisor が exp 002 の Kaggle 提出 (id 52716957) の publicScore を確認、ローカル高 winrate と LB regression の乖離を発見。
  - 防止策: submit gate を mix-eval (H021) に格上げ。**no-regression 方式**: `winrate_random≥baseline_random AND winrate_sniper≥baseline_sniper AND winrate_prev_best≥baseline_prev_best+0.05 AND winrate_min≥0.50 AND errors_total=0` の全条件を満たさないと submit しない。baseline は best_score.json mix_eval から動的読込 (hardcode 回避)。特に `winrate_min≥0.50` で「どの相手にも負け越さない」を保証。
  - 補足 (exp/021b, supervisor 通知2): 当初の絶対値 gate (random≥0.90, sniper≥0.60) は現 baseline (random 0.8667) すら通らないため no-regression に変更。「baseline から悪化しない + prev_best のみ +0.05 改善」で、submit ごとに単調改善を保証しつつ baseline を不当に弾かない。
  - 関連: LB score は時間変動する (episode 蓄積で再計算: 52716957 は 360.9→393.4、52032932 は 383.5→410.7)。go/no-go 判定は提出直後でなく **24-48h 後の安定 score** で行う。
- AVOID: `eval_term_redundant_with_production` — production を含む補助 eval 項 (例: 30 turn projection_total) は既存 `production*8` と方向が同じで補完にならず、over-expansion を促して強い相手に regression する。
  - 検出契機: exp/002 projection (H002)。PROJECTION_WEIGHT 0.3/0.1 で mix-eval すると random は改善 (0.93-0.97) するが nearest_sniper 0.5333 / prev_best 0.3667 に regression、winrate_min 0.3667 で gate fail。
  - 重要: projection_total は全 own planet への**スカラー一律加算**のため、係数を変えても beam の argmax (選ぶ action 列) が同一になり **sweep が無効化**される (0.3 と 0.1 で prev_best が完全同一 11/30)。係数を下げても救えない項は方向そのものが誤り。
  - 防止策: 新 eval 項は既存項と**方向が異なる別軸** (territory=空間支配 / threat=守備減算 / sun blocking) を選ぶ。production 系の項を足す場合は二重カウントにならない定式化 (最弱 planet の projection、threat 込み差分等) にする。
- INFO: `new_eval_term_helps_random_hurts_strong` — 新規 eval 補助項は random (弱い相手) で winrate を上げつつ nearest_sniper / legacy-388 (強い相手) で regression する傾向が強い。**単独の random 改善は効果の証拠にならない**。go/no-go は gate の `winrate_min` と `prev_best` (強い相手) で判定すること。
  - 検出契機: 3 例連続で同パターン。exp/002 territory (LB -17.3)、exp/002proj projection (random 0.93-0.97↑ / sniper 0.5333↓ / prev_best 0.3667↓)、exp/003 threat-eta (random 0.8667→0.9667↑ / sniper 0.5667→0.5333↓ / prev_best 0.5667→0.4667↓、winrate_min 0.4667 で gate fail)。
  - 解釈: random は弱いので攻撃寄り/雑な eval でも勝てるが、強い相手は守備の隙や over-expansion を突く。random winrate は天井 (>0.90) に張り付きやすく識別力が低い。
  - 防止策: hypothesis の go 条件に random を含めない (no-regression の下限のみ)。改善を主張する相手は必ず prev_best / nearest_sniper にする。新 eval 項は強い相手で測ってから採否を決める。
- INFO: `deeper_search_overfits_wrong_opponent_model` — beam の探索深度を上げる (SEARCH_DEPTH 2→3) と弱い相手で改善 (random 0.8667→0.9333 / nearest_sniper 0.5667→0.6) しつつ強い相手 prev_best (legacy-388) で 0.5667→0.4333 と regression する。`new_eval_term_helps_random_hurts_strong` と同じ helps-weak/hurts-strong パターンを **eval 項でなく search depth 経由**で示す 4 例目。
  - 検出契機: exp/004 (H004d) depth=3。timing は安全 (max 27ms<<700ms budget) だが mix-eval winrate_min 0.4333<0.50 で gate FAIL → discard。
  - 根本原因: beam の opponent simulation (`_simulate_opponents` → `_phase1_decisions` heuristic) が legacy-388 の実挙動と乖離している。深く読むほどこの**誤った opponent モデルへ過適合**し、実際の強い相手に対し読み筋がズレて守備/攻撃判断が悪化する。depth=2 は浅いため過適合が浅く害が小さい。
  - 重要: budget/width はレバーでない。depth=2/width=16 で beam は max 6ms (budget 300ms) しか使わず、width sweep (16/24/32) は frontier 飽和で timing 完全同一 = no-op。search 強化の唯一の実効軸は depth、だが depth は opponent モデル精度に律速される。
  - 防止策: search depth を上げる前に opponent モデルを実挙動に近づける (H011 PUCT-MCTS の rollout policy、H013 opponent classifier)。正しい opponent モデルが無い段階での depth 増は強い相手で逆効果。
- INFO: `opponent_model_not_root_cause_of_depth_regression` — H023 (exp/023) で `deeper_search_overfits_wrong_opponent_model` の「opponent モデルが真因」仮説を直接検証 → **反証寄り**。`_simulate_opponents` の opponent decision を `_phase1_decisions` (純 greedy phase1) から beam-at-depth-1 argmax (`_opponent_best_actions`: beam 自身の `_candidate_actions_for_planet` + opponent 視点 `_score_state` の argmax) に強化し depth=3 で mix-eval。
  - 結果: prev_best (legacy-388) は H004 (depth=3+旧 opponent) 0.4333 → 0.4667 と **+0.0334 (≒1 試合) しか改善せず**、baseline 0.5667 にも gate 0.6167 にも遠く届かない。random 0.9333 / nearest_sniper 0.6 は改善するが winrate_min 0.4667<0.50 で gate FAIL → discard。
  - 解釈: opponent の decision model を beam 同等に強化しても depth=3 の strong-opponent regression はほぼ解消しない。よって helps-weak/hurts-strong の主因は opponent decision model ではなく **downstream** にある。最有力候補は search の **fleet projection 凍結** (beam.py `search()` で raw obs 由来の敵 fleet を target_planet_id=-1 / turns_remaining=99 に固定 = 探索内で敵 fleet が永遠に到達せず空中孤島化、L488-490 の既知 v0 簡略化)。深く読むほどこの「敵 fleet が消える」誤った dynamics に過適合する。
  - 防止策: depth/MCTS で探索を強化する前に、まず fleet の target 推定 (最近接 enemy/neutral への進行と仮定) + arrival turn 計算を実装し、探索内の game dynamics を実環境に近づける。opponent decision model の更なる強化 (classifier 等) は優先度を下げる。
- INFO: `fleet_projection_freeze_not_root_cause` — H024 (exp/024) で `opponent_model_not_root_cause_of_depth_regression` が指した最有力候補「fleet projection 凍結 (target=-1/turns_remaining=99)」を直接修正 → **反証**。in-flight fleet の進行 angle に最も合致する非自軍 planet を target 推定し ETA で探索内到達させる最小実装 (depth=2/eval は baseline 据置)。
  - 結果: random 0.8667→0.9333↑ / nearest_sniper 0.5667→0.6↑ / **prev_best (legacy-388) 0.5667→0.4667↓**、winrate_min 0.4667<0.50 で gate FAIL → discard。H024 自身の parity 基準 (prev_best が baseline 未満に shift したら targeting wrong) に抵触。
  - **決定的観測**: random/nearest_sniper/prev_best の wins カウントが H023 (opponent model 強化+depth=3) と完全一致 (28/18/14)。互いに独立な 2 変更 (opponent model / fleet projection) が legacy-388 相手に同一の regression を生む = この regression は systematic で、特定 knob のバグではない。
  - 解釈: 探索内 dynamics を「より正確」にする (fleet 着弾を入れる) 行為そのものが legacy-388 相手に逆効果。baseline (depth=2 + fleet freeze) の prev_best 0.5667 は freeze による浅い読みが legacy-388 (= prev_best = main の元 baseline) とミラー的に噛み合っているだけの可能性。eval×3 / depth / opponent-model / fleet-projection の **5 系統全て**で同一 helps-weak/hurts-strong。
  - 防止策: beam の個別 knob (depth/eval/fleet/opponent) を逐次改善する路線を打ち切る。(a) H011 PUCT-MCTS で探索パラダイムごと置換する、(b) prev_best を legacy-388 単体でなく多様な強相手 mix にして「legacy-388 ミラー特性」と「真の強さ」を切り分ける、のいずれかを先に行う。単一の legacy-388 を prev_best に据えた no-regression gate は、legacy-388 に過適合した baseline を不当に有利にしている恐れがある。
- INFO: `paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` — H011 (exp/028) で `fleet_projection_freeze_not_root_cause` が推奨した「H011 PUCT-MCTS で探索パラダイムごと置換」を直接検証 → **反証**。beam の SearchState/_phase1_decisions/_score_state/_simulate_opponents/_expand_turn を再利用しつつ root-level UCB1 木 + phase1 rollout (depth=2) + visit-count argmax の純 MCTS minimal を実装、budget=0.3s で mix-eval。
  - 結果: random 0.8667→**1.0**↑ / nearest_sniper 0.5667→**0.20**↓↓ / prev_best 0.5667→**0.1667**↓↓、winrate_min(random,sniper)=0.20<0.50 で relaxed gate FAIL → discard。helps-weak/hurts-strong の **6 系統目** (eval×3 / depth / opponent-model / fleet-projection / paradigm-swap)。
  - **決定的観測**: パラダイム (beam → MCTS) を置換し探索の data flow を完全に変えても、strong opponent (sniper/legacy-388) の regression が逆に**さらに悪化** (depth-3 系の 0.4-0.47 → MCTS の 0.17-0.20)。共通の rollout policy/eval (phase1_decisions + _score_state) を共有している以上、探索の形を変えても根本症状は解消しない。
  - 解釈シフト: 真因は探索アルゴリズム (個別 knob・木構造のどちらでも) ではなく **rollout policy + eval + measurement design の組合せ**にある可能性が濃厚。特に勝敗判定 sigmoid (score→[0,1]) が score 大絶対値で飽和し木の visit 分布が荒れる、phase1 rollout が strong opponent と相性悪く noise を増幅する、prev_best=legacy-388 単体 gate が baseline に過剰偏向、のいずれか/組合せ。
  - 防止策: 次手は (a) prev_best mix を多様化して測定 degeneracy を切り分け、(b) rollout policy を phase1 でなく random/uniform に置換した A/B 比較、(c) eval (`_score_state`) の絶対スケールと sigmoid の組合せを再設計、のいずれかを先に行う。MCTS の知識集約 (tree, progressive widening, RAVE) は (a)-(c) で根本症状を切り分けた後に再導入。
- INFO: `mcts_rollout_policy_not_root_cause` — H026 (exp/030) で上記 (b)「rollout policy を phase1→random/uniform に置換した A/B」を直接検証 → **反証**。mcts.py `_rollout_value` に `ROLLOUT_POLICY` env を追加し uniform rollout (各自軍 planet の候補 action から一様ランダム選択、wait も等確率) を実装、`ORBIT_WARS_MCTS=1 ROLLOUT_POLICY=uniform` で mix-eval。
  - 結果: uniform random 0.9667 / nearest_sniper 0.30 / prev_best 0.1333 / winrate_min 0.1333 vs phase1 (exp028) random 1.0 / sniper 0.20 / prev_best 0.1667 / winrate_min 0.1667。**strong-opponent regression はほぼ不変** (sniper 0.20→0.30 で +1試合、legacy 0.1667→0.1333 で -1試合、いずれも noise レンジ)。両方 winrate_min ≪0.50 で gate FAIL。
  - 解釈: MCTS の strong-opponent 激減は rollout policy (greedy phase1 か uniform random か) に依存しない。(a)(b)(c) のうち **(b) を消し込み**。残る真因候補は (a) measurement (prev_best=legacy-388 単体)、(c) eval×sigmoid scale、または MCTS の root-only 木構造そのもの (depth=1 で敵応手を読まず visit が全 child に薄く分散)。
  - 重要 (measurement の健全性 — (a) への反証寄り証拠): ledger の LB 付きデータでは exp025 (sniper 0.6↑ / legacy 0.4667↓) が **LB 378.2 (-36)**。legacy-388 (prev_best) 軸は LB regression を正しく予測しており、sniper 軸だけ見ると exp025 を PASS させ trap を踏む。「legacy 単体 gate が baseline を不当有利化 ((a) の前提)」は LB データと整合しない。よって次の最有力は (c) eval×sigmoid、または「baseline 414.2 が heuristic beam の強い local optimum で、個別改変では超えられない (Phase 3 NN が必要)」。
  - 防止策: helps-weak/hurts-strong は 7 系統 (eval×3/depth/opponent/fleet/paradigm) + rollout-policy で消し込み済。MCTS 個別 knob (rollout/木/sigmoid) の逐次改善より、(c) eval スケール再設計の単独 A/B か、Phase 3 (H016 NN value head) へ移行を優先。baseline beam 414.2 は frozen 維持。
- INFO: `avoidance_robustification_noise_level_no_gain` — H005 (exp/031) で `avoidance_angle` (src/strategy/geometry.py) の単一 12° stub を robust 化 (偏向後経路が sun を抜けるまで 12°→90° を段階増加、12° で抜けるケースは従来と完全同一出力、出力 move 角度のみで beam 内部 sim 不変)。helps-weak/hurts-strong の **外側**にある初の変更を検証。
  - 結果: 2 回独立測定 (2026-05-28 / 2026-05-30) で random 0.8667〜0.9333 / **nearest_sniper 両回 0.5333 (16勝)** / prev_best 0.6〜0.7333。strong opponent (prev_best=legacy-388) は両回 baseline 0.5667 以上を維持し **regression せず** = 探索/eval 非関与の bounded change は strong を壊さないという advisor 仮説と整合。
  - しかし sniper が baseline 0.5667 (17勝) を **-1 試合 (16勝)** 下回り per-opponent no-regression gate FAIL → no-submit・discard。effect 自体が noise レベル (random 不変〜+2試合 / prev_best +1〜+5試合 / sniper -1試合) で正味 gain なし。geometry.py は baseline に revert し parity 維持 (LB 414.2 不変)。
  - 解釈: 「探索/eval を触らない bounded change は strong を壊さないが gain も生まない」。mix-eval opponent set では sun-crossing near-tangent 経路の発生頻度が低く、理論上の改善 (fleet を sun に捨てる失敗の削減) が measurable gain に至らない。
  - 防止策: bounded・no-downside と見込まれる単一 knob 改善でも、N=30 では 1 試合 noise で strict gate を割りうる。go 条件に「明確な勝率 gain (≥+2〜3 試合 on prev_best/sniper)」を要求し、noise レンジの neutral 変更は最初から候補にしない。
- INFO: `local_cpu_selfplay_export_infeasible_at_scale` — H016 NN value 学習データのローカル CPU export は実用規模で非現実的。exp/037 で計測: `export_value_data.py --games-per-opponent 2` (baseline beam main.py vs random/nearest_sniper/prev_best mix, 6 games) が **real 443.71s = ~74s/game**、stride=4 で 1326 samples。
  - 外挿: 実用的 value dataset (210K-param transformer の汎化に最低でも数千 game) → 1000 game ≈ 20.5h、5000 game ≈ 100h+。headless local loop の 1 iter (CPU only, ローカル GPU 学習禁止) では生成不能。
  - 解釈: H016 の真の bottleneck は実 self-play data 生成 + Kaggle GPU 学習という **compute** であり、local loop の src/ 編集サイクルの範囲外。infra (encoder/exporter/model/train/quantize/MCTS 配線) は紙の上で完成しても、実モデルなしには「NN value が MCTS strong-opponent regression を治すか」というコア signal が出ない (infra completeness theater のリスク)。
  - 防止策: 実データ生成 + Kaggle GPU 学習は **supervisor の gating item として escalate** (kaggle-runner で notebook push&run、quota.json で 30h/week 追跡)。local iter は配線契約の固定 (ONNX input names / encoder skew 最小化) までを担い、実モデルは後で差す。NN 路線の go/no-go は実モデル統合後の mix-eval (winrate_min≥0.50 で sniper/prev_best が 0.20/0.17 → baseline 付近に戻るか) で判定。real gain が疑われる場合のみ N≥100 で再測。
- INFO: `mixeval_prevbest_gate_rejected_lb_winner` — mix-eval no-regression gate (特に prev_best=legacy-388 軸) は **LB 改善する提出を弾く** 失敗モードを持つ。exp029 (H025: beam `_simulate_opponents` を noop 化) は local mix-eval で random 0.8667→0.9↑ / nearest_sniper 0.5667→0.6↑ だが **prev_best 0.5667→0.4667 (-1試合) で no-regression gate FAIL** 相当だったにもかかわらず、relaxed gate で提出 → LB **435.3 (+21.1 vs baseline 414.2)** で 4月以来初の実 LB 改善を達成。
  - 検出契機: 2026-05-30 reconciliation iter。PR #60 は merge 済 (HEAD 祖先) だが best_score.json/hypotheses/ledger が 414.2/52032932 で stale だったため LB 確定を同期した際に発覚。LB table + leaderboard 両方で 435.3 安定確認 (提出 53126719, 2026-05-28, 48h+ 経過)。
  - **解釈の転換**: これまで「mix-eval prev_best が LB を予測する」と解釈してきた (learned_rules `single_opponent_winrate_not_lb_predictive` の補強で exp025 prev_best 0.4667→LB -36 を根拠) が、exp029 は同じ prev_best 0.4667 で **逆に LB +21**。prev_best=legacy-388 単体軸は **LB と単調関係にない** (regression も improvement も同じ 0.4667 を取りうる)。mix-eval gate は「弱い提出を弾く」だけでなく「強い提出も弾く」両方向に誤判定する。`accept bad submissions` より `reject good submissions` の方が探索を停滞させる危険な失敗モード。
  - 解釈 (なぜ noop が効くか): beam の探索内 opponent モデル (`_phase1_decisions` per turn rollout) は実環境の相手挙動と乖離 (learned_rules `deeper_search_overfits_wrong_opponent_model` / `opponent_model_not_root_cause...`)。opponent を **静止扱い (noop)** にする方が、誤った動的モデルで読むより LB 上の実プレイで優位。「より正確な dynamics ほど良い」は誤りで、`fleet_projection_freeze_not_root_cause` の決定的観測 (freeze 版 baseline が legacy ミラーで噛み合う) と整合 = 探索内 dynamics を単純化する方向に gain がある。
  - 防止策: (1) mix-eval gate を **submit を止める hard gate でなく advisory** に格下げ検討 (winrate_min≥0.50 と errors=0 の安全条件のみ hard、prev_best/no-regression は warn)。(2) gate FAIL でも quota に余裕がある時は **LB で答え合わせ**する価値がある (local 信号が両方向に誤る以上、真の judge は LB)。(3) prev_best=legacy-388 単体を go 条件から外し、LB delta を唯一の go/no-go とする。
- INFO: `mixeval_prevbest_gate_noisy_misjudges_lb` — 上記 `mixeval_prevbest_gate_rejected_lb_winner` の追補 (2026-05-30 reconciliation で fresh 再測定して判明)。exp029 noop beam の **同一コード**を 2 回測定して prev_best が 0.4667 (14勝, 2026-05-28 提出時) → 0.7 (21勝, 2026-05-30 再測定) と **range 0.23 (7試合)** 振れた。LB は +21 の単一真値。
  - 解釈: N=30 mix-eval の per-opponent winrate は二項分散が大きく (σ≈√(p(1-p)/30)≈0.09、95%CI 幅 ≈0.34)、単一測定で no-regression gate (1試合差で割れる) を判定するのは統計的に無効。これまでの helps-weak/hurts-strong の「1試合 regression で gate FAIL」判定の多く (exp/031 sniper -1試合, H024/H023 等) は **noise と真の effect が分離できていない** 可能性。
  - 防止策: gate 判定に使う winrate は最低 N=100 (σ≈0.05) を要求するか、go/no-go を LB delta 単独にする。N=30 は方向性の sanity check (errors=0 / 全相手 winrate>0.3 等の安全フィルタ) に留め、±1-2試合の差で採否を決めない。learned_rules の過去 helps-weak/hurts-strong 系 INFO は「1試合差 FAIL」を含むものほど信頼度を割り引く。
  - **追補 (H030/exp045, 2026-05-31): N=100 で真値確定**: 現 main (noop beam, byte-identical) で prev_best を **N=100 で 1 回測定 → 51/100 = 0.51**。N=30 で振れた 0.4667 と 0.7 は**両側とも真値 0.51 から大きく外れた noise** だったと確定。legacy-388 mirror は現 agent 相手に **ほぼコインフリップ (0.51)** = gate decision 軸として現 agent 周辺で識別力ほぼゼロ。N=100 でも σ≈0.05 のため ±1σ 改善 (0.56) の検出すら実用限界 — beam 単一 knob の go/no-go を local mix-eval で測ること自体が無効、真の judge は **LB delta 単独**を再確認。安定値は best_score.json `prev_best_n100_baseline` に記録、infra は `tournament.py --prev-best-n`。
  - **波及**: 現行 gate baseline (best_score.json mix_eval prev_best 0.7) は noisy N=30 値。これを基準にした過去/並行判定 (特に H016 NN cycle 1-3 の prev_best -7/-8g 'regression') は inflated baseline との比較で、真の baseline 0.51 では NN cycle の prev_best 0.4333-0.4667 は -0.04〜-0.08 (≈1σ前後) の noise レンジに収まる。NN go/no-go は baseline 0.51 で再評価する価値がある (supervisor 領分)。

- INFO: `comet_reactive_targeting_no_gain` — H006 (exp/039) で comet pre-positioning を reactive comet targeting に pivot (env source 実測: spawn step [50,150,250,350,450] は固定だが spawn 位置は episode_seed scrub で予測不能、orbit_wars.py L347「Agents must not be able to reconstruct the comet schedule」)。comet は spawn 時 neutral planet として comet_planet_ids で可視だが eval で完全無視 = 未探索 signal のため、score_target (targeting.py) に COMET_TARGET_BONUS=6.0 の target 選択 bias を付与 (additive eval scalar 回避、advisor 助言)。
  - 結果 (fresh mix tournament N=30/相手): random 0.9333(28) / nearest_sniper 0.6(18) / prev_best 0.3667(11) / errors=0。真の noop-beam baseline (0.9667/0.6/0.7) 比で random -1g (no-regression FAIL) / sniper = / **prev_best -10g (0.7→0.3667, collapse floor 直上)**、全相手で upside ゼロ。stale legacy baseline (0.8667/0.5667/0.5667) では gate PASS だが artifact。
  - 解釈: `avoidance_robustification_noise_level_no_gain` と同型の no-gain bounded change。comet は安価/近接時のみ tip する bias だが mix-eval games で comet が決め手になる頻度が低く、理論上の capture/denial 価値が measurable gain に至らない。prev_best collapse は N=30 noise (mixeval_prevbest_gate_noisy_misjudges_lb) も寄与しうるが upside ゼロのため submit 価値なしと判断。
  - 防止策: comet を活かすなら「可視後の reactive bias」でなく「comet を含む長期 horizon の探索内 dynamics」が要るが、それは helps-weak/hurts-strong 系の探索強化路線 (8 系統 kill 済) に戻る。Phase 1 beam の局所最適 435.3 は単一 knob では超えられない追認、Phase 3 NN (H016, supervisor GPU escalate) 優先を再確認。
- INFO: `depth3_noop_regime_below_n30_detection` — H027 (exp/042) で depth×opponent matrix の唯一の空白セル「noop opponent baseline 上の SEARCH_DEPTH=3」を測定。過去の depth=3 (H004/H023) は全て phase1/argmax opponent model 下で prev_best が 28/18/14 系の systematic regression を示したが、現 noop regime (`_simulate_opponents=noop`, H025 で LB +21) では未測定だった。
  - 結果 (fresh mix-eval N=30/相手): random 0.9(27) / nearest_sniper 0.6(18) / prev_best 0.5(15) / errors=0。gate FAIL (random 0.9<official baseline 0.9667)。
  - **決定的観測 (directional read が inconclusive)**: この 27/18/15 は exp041 ledger に記録された **同日 byte-identical depth=2 baseline 再測定 (random 0.9/sniper 0.6/prev_best 0.5 = 27/18/15) と完全一致**。depth=2 の run-to-run 変動レンジ (official 29/18/21 ↔ exp041 27/18/15) 内に depth=3 が収まる。tournament の beam search は time-budget 律速で 7-worker 並列下の CPU load により run 間変動があり、**N=30 では depth=3 と depth=2 baseline を統計的に分離できない**。crater (≤14, 過去 phase1-depth3 パターン) でも hold (~20-21) でもなく「検出閾値以下」。
  - 解釈: noop regime で depth=3 が「害」とも「無害」とも N=30 では結論できない。`mixeval_prevbest_gate_noisy_misjudges_lb` (同一コードで±7試合振れる) の直接追認 — byte-identical baseline 自身が 29↔27(random)・21↔15(prev_best) と振れる以上、単一 knob の ±数試合差を N=30 で採否判定するのは無効。advisor が事前提示した crater/hold の 2 値判別は、baseline 自体の測定分散がその分離幅を飲み込むため成立しなかった。
  - 防止策: (1) depth 路線 (noop regime 含む) は N=100+ でなければ判定不能、かつ過去 8-9 系統の単一 knob 全 kill と整合して LB upside の事前期待が低いため close。(2) 今後 beam 単一 knob の go/no-go を N=30 mix-eval で測ること自体を避け、判定が必要なら LB delta 単独 or N≥100。(3) baseline 自身を毎回 fresh 同時測定して control に使わない限り、N=30 の 1 回測定は knob 効果の証拠にならない。Phase 1 beam 局所最適 435.3 を超える escape は Phase 3 NN (H016, supervisor GPU escalate) のみという結論を再々確認。
- INFO: `tournament_noise_from_wallclock_cutoff_under_parallel_load` — H028 (exp/043) の primary-source 診断で `mixeval_prevbest_gate_noisy_misjudges_lb` の **機構**を特定。同一 agent・同一 seed・**単独逐次**実行の match.py は 2 回とも winner/rewards 完全一致 (seed3: winner0 [1.0,-1.0] 両回、elapsed 276s↔273s) = match 自体は決定論的、env/opponent RNG はノイズ源でない。
  - 真因: beam.search は wall-clock time budget (`time.perf_counter() - started_at > time_budget_sec`) で探索を打ち切り、超過時は `fallback_moves` (phase1) を返す。mirror probe (seed3, 単独) で beam が **267/300 turn で >240ms、median 308ms/max 589ms** と 0.3s budget を恒常超過。tournament は ProcessPool で N 並列実行するため、**7-worker CPU 競合下では各 process の wall-clock budget が run ごとに変動 → cutoff 点 (beam完走 vs phase1 fallback) が turn 単位で変わり → 同一 seed でも move が変わる**。これが byte-identical baseline が 29↔27(random)/21↔15(prev_best) と振れる正体。seeds は毎回共通 (0..14) なので CRN seeds では治らない。
  - 防止策: (1) N=30 mix-eval の ±数試合差は CPU-load ノイズであり knob 効果でない。go/no-go は LB delta 単独 (確定済)。(2) 決定論的 A/B が必要なら tournament を low-worker (競合排除) or beam を expansion-count budget (wall-clock 非依存) 化。後者は agent hot-loop 改変で DQ リスクあり要慎重。(3) 過去 helps-weak/hurts-strong 系の「±1試合 FAIL」判定は本ノイズを knob 効果と誤認した可能性が高く、信頼度を割り引く (`mixeval_prevbest_gate_noisy_misjudges_lb` 補強)。
- INFO: `beam_budget_underutilizes_acttimeout` — H028 (exp/043) 診断: 提出 agent は Kaggle actTimeout **1s** (main.py deadline 0.90s, margin 100ms) に対し `_BEAM_TIME_BUDGET_SEC=0.3` で beam を 0.3s に self-cap していた = **利用可能 budget の 1/3 のみ使用**。dense/late turn では beam が 0.3s を超過して打ち切られ phase1 fallback に落ちる頻度が高い (median は state 依存で sparse turn 95ms ↔ dense turn は cap 律速)。
  - 実測 (`main.agent` full path, seed7, 300turn, 単独): budget 0.3 → max 483ms / 0.5 → max 601ms / 0.6 → max 748ms、いずれも median 95ms・over900=0。budget は dense/pivotal turn の探索深度にのみ効き、max は budget+~100-150ms (time-check granularity の overshoot)。
  - 対応 (exp/043): `_BEAM_TIME_BUDGET_SEC` を 0.3→**0.5** に引き上げ (max 601ms、1000ms hard limit に ~400ms margin、950ms CI assert pass)。検証済 noop beam (LB 435.3) に pivotal turn でより深い時間を与える。これは 9 系統 kill の「新 eval/depth/paradigm 追加」とは別カテゴリ (既存 beam に本来の compute を供給)。LB が真の judge。
  - **supervisor escalate**: budget を 0.6+ に上げる / time-check を細粒度化して overshoot を削り更に上げるのは、Kaggle 実機の per-turn latency telemetry (ローカル M-series と CPU 速度差不明) がないと DQ (絶対禁止) リスクを bound できない。0.5 は local max 601ms から保守的に選定。LB +改善が確認できたら次段の budget 引き上げを Kaggle latency 計測込みで検討。

- INFO: `anytime_beam_no_gain_vs_strong` — H029 (exp/044) で beam.search() の hard-timeout 分岐 (beam.py:544/555) を anytime 化 (timeout 時に phase1 greedy fallback でなく完了済 depth の best_frontier=`_score_state` argmax を返す)。`beam_budget_underutilizes_acttimeout` で dense/late turn (267/300) がこの timeout 経路 hot と判明していたため、phase1 fallback が 435.3 の load-bearing 要素か (= dense state で beam をより信頼すると strong opponent で crater するか) を測定。**matched A/B (prev_best のみ N=60)**: anytime 34/60 (0.5667) vs baseline 38/60 (0.6333) = **-4g (-0.0667 ≈ 1σ未満)**。N=30 初回 anytime 13/30 (0.4333) は noise (N=60 で 0.5667 に回帰、`mixeval_prevbest_gate_noisy_misjudges_lb` 再現)。**結論: phase1 fallback の beam 置換は strong opponent で非改善・僅か下方** = helps-weak/hurts-strong の縁、phase1 fallback は drag でなく load-bearing 寄り。beam に dense-turn 影響を増やす方向 (anytime / H028 budget 増) は strong-opponent upside を生みにくいと示唆 (H028 de-risk material)。beam.py baseline revert (agent bit-identical, 提出 435.3 維持)、no-submit。単一 knob kill の 10 系統目。escape は依然 Phase 3 NN (H016, supervisor GPU) のみ。

- INFO: `actTimeout_full_use_regresses_strong_opponent` — H028 (exp/043) の **LB 実証**: `_BEAM_TIME_BUDGET_SEC` を 0.3→0.5 に引き上げる supervisor override submit (submission 53299379, 2026-06-02) は 22h 経過で publicScore **401.7** に到達 (初期 600.0 spike から collapse、H001 territory の 600→394 と同パターン)。baseline 435.3 比 **-33.6 で明確な regression**。
  - 解釈: 既存 beam に compute を供給 (5/3 倍時間) するだけの bit-identical-knob 変更でも strong opponent で regression する。H029 anytime-beam 診断 (`anytime_beam_no_gain_vs_strong`、prev_best -4g) が「beam に dense-turn 影響を増やす方向は strong-opponent upside を生まない」と事前示唆していたのを LB が追認。phase1 fallback は drag でなく **load-bearing**、frozen 0.3 が局所最適。
  - 統合: 単一 knob kill **11 系統目** (eval×3 / depth / opponent-model / fleet-projection / paradigm-swap MCTS / rollout-policy / sun-avoidance / multi-launch / anytime-beam / **budget-raise**)。Phase 1 beam の局所最適 435.3 を超える escape は Phase 3 NN (H016, supervisor GPU) のみ。
  - 防止策: (1) main.py の `_BEAM_TIME_BUDGET_SEC` を 0.5→0.3 に revert、agent を bit-identical noop baseline (LB 435.3) に戻す。(2) 「compute 供給だけの bit-identical knob」も submit 前に H029 anytime-beam のような diagnostic A/B (matched N=60+ prev_best) を hard requirement にする (supervisor override も例外でない)。
  - 関連: `supervisor_resurrect_against_stale_baseline` (H001 同型 600→394 collapse パターン)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N=30 では分散吸収のため判定不能、N=60+ 必須)、`anytime_beam_no_gain_vs_strong` (事前 de-risk material を override で無視した supervisor 判定ミス)。

- INFO: `kaggle_p100_pytorch_incompat` — H016 cycle 1 (2026-06-04, exp/046) で Kaggle GPU notebook `orbit-wars-nn-value-train` の train 段階で **CUDA error: no kernel image is available for execution on the device**。Kaggle が割り当てた **Tesla P100 (sm_60)** と現 Kaggle image の **PyTorch (sm_70+ のみサポート)** が non-compatible。data-gen (CPU 並列) は完走、train 直前で fail。
  - 対応 (cycle 1 で実施): data-gen 出力 `value_dataset.npz` (91234 samples, 510 games) を `kaggle kernels output` で local pull、**local CPU で train_value.py + quantize_onnx.py を完走** (M-series ARM、30 epochs ~15min)。INT8 sign_agreement 100%、quantize 結果 main 提出 path から見れば bit-identical な fallback。
  - 防止策: 次 Kaggle GPU 起動前に kernel-metadata.json で GPU type を明示 (T4/V100)。kaggle CLI の `--gpu-type` 指定可否は要確認、無理なら `enable_gpu: false` で CPU 専用にして data-gen のみ Kaggle、train は local で完結。
  - 関連: `local_cpu_selfplay_export_infeasible_at_scale` (Kaggle 並列で打開する設計だったが train だけ GPU 依存の脆弱性顕在化)。

- INFO: `nn_value_distribution_shift_legacy388_mirror` — H016 cycle 2-3 (2026-06-04, exp/047, exp/048) で NN value head (val_sign_acc 0.7611, train_acc 0.9663) を beam の `_score_state` env-gated 加算 (`ORBIT_WARS_NN_VALUE_BEAM=1`) して N=30 mix-eval:
  - cycle 2 (WEIGHT=1.0): random 0.9333 / **sniper 0.6 = baseline tied!** / prev_best **0.4333 (-8g vs baseline 0.7)**
  - cycle 3 (WEIGHT=0.3 sweep): random 0.9 / sniper 0.6 / prev_best **0.4667 (-7g)** = cycle 2 と差 1g (noise レベル)
  - **真因**: train data (`export_value_data.py --agent main.py = noop-beam vs noop-beam self-play`) と query (mix-eval で noop-beam vs **legacy-388 mirror**) の **distribution shift**。legacy-388 は noop-beam より強いので「noop-beam が劣勢」な state が多発、NN は劣勢 state 評価が不正確 (train_acc 0.97 vs val_acc 0.76 の 20pt gap も整合)。
  - weight sweep (w=1.0 vs w=0.3) で差 1g = 効果ほぼ同じ = **weight tuning は dead end**。NN value 自体の質が legacy-388 mirror state に対し不足。
  - sniper (= competition-starter) では tied (0.6 = baseline) → noop-beam self-play で **soft target distribution は学習できている**が、強敵 legacy-388 への generalization は限定的。
  - 次手 (cycle 4 候補): **mixed-opponent self-play data 生成** = `export_value_data.py` を legacy-388 戦も含めて再 run、distribution gap を埋める。GPU は P100 incompat (上記) のため T4 指定 or local CPU 30h+ run (締切 18 日との trade-off 要 user 判断)。
  - 関連: `tournament_noise_from_wallclock_cutoff_under_parallel_load` (cycle 3 で w=1.0 vs w=0.3 の 1g 差は noise floor 内)、`mixeval_prevbest_gate_noisy_misjudges_lb` (cycle 結果は N=60 matched A/B で確証要、ただし N=30 σ=0.089、Δ=0.27 は明白 CI 外)。
  - 重要 framing (advisor 助言): **NN value を「kill 12 系統目」と framing しない**。MCTS+NN cycle 1 (exp 028 比 sniper 0.20→0.47, prev 0.17→0.33 で 2× 改善) と NN-in-beam cycle 2 (sniper baseline 完全一致) は NN が positive signal を持つ evidence。kill 12 とすると有用な asset を棄却することになる。asset は repo 外 backup 済 (`~/orbit-wars-models/cycle1-2026-06-04/`)、cycle 4 で再利用。
  - **追補 (H016 cycle 5, exp/049, 2026-06-24): merged data で distribution shift 解消を確認、ただし NN-in-beam は neutral (gain なし)**。cycle 4 で生成した merged dataset (`value_dataset_merged_c1_c4.npz` = cycle1 mix 510 + cycle4 legacy-388 300 = 810 games、legacy-388 戦 ~58%) で local CPU 再学習 (val_sign_acc 0.7045、cycle1 0.7611 より低下 = legacy-388 mix で states 多様化し fit 難化) → int8 quantize (sign_agreement 1.0) → NN-in-beam weight=1.0 mix-eval: random 0.9333 / sniper 0.6 / **prev_best 0.50 (15/30)**。
    - **distribution shift 解消の追認**: cycle 2 (cycle1-only data) の prev_best 0.4333 (-8g) → cycle 5 (merged) 0.50 = N=100 baseline 真値 0.51 とほぼ同値に回復。legacy-388 戦 data を足すと劣勢 state 評価が改善し regression が消える = `nn_value_distribution_shift_legacy388_mirror` の真因 (train/query distribution gap) が正しかったことを実証。
    - **しかし neutral = gain なし**: 3 相手とも baseline tie (random 0.9333 / sniper 0.6 / prev_best 0.50≈0.51)。go 基準 (cycle4 plan: prev_best 0.6+) 未達。NN value は beam の `_score_state` を**上回らない** (additive weight 加算では beam の既存 eval に何も足せていない)。**NN-in-beam additive (weight 加算) は dead-end 確定**。
    - **latency 警告**: NN inference で mix-eval tournament が ~26min (baseline 数分の ~6x)。submission の actTimeout 1s/turn で NN-in-beam を載せると DQ リスク大、`assert max_turn_ms<900` の事前検証が hard requirement。
    - 防止策/次手: (1) weight sweep (cycle3) も data 増量 (cycle5) も NN-in-beam additive を gain にできず = beam additive 路線は close。(2) NN の本命は **MCTS leaf eval** (cycle1 で exp028 比 2× 改善実績) = beam の _score_state と競合させず MCTS の弱い leaf eval を置換する用途。ただし MCTS 自体が exp028 で strong-opponent 激減 (sniper 0.20) のため MCTS+NN を baseline 435.3 超えまで持っていくには更に cycle 要。(3) NN value 自体の質向上 (より強い teacher の self-play data、policy head 追加 H017) も候補。(4) いずれも submission 前に latency gate (max_turn_ms<900) 必須。asset backup: `~/orbit-wars-models/cycle4-2026-06-05/` + checkpoint `experiments/checkpoints/value_net_c5.*`。

- AVOID: `nn_in_mcts_leaf_local_eval_infeasible_in_harness` — H016 cycle 6 (2026-06-24, exp/050) で merged-data モデル (`value_net_c5.int8.onnx`, distribution shift 解消済) を MCTS leaf eval (`ORBIT_WARS_MCTS=1 ORBIT_WARS_NN_VALUE=1`) に通し N=30 mix-eval を試みたが、**ローカル harness で完走できず統計 signal を取得できなかった**。
  - 観測: NN-in-MCTS は per-game wall-time が極端に大 (単発 match vs prev_best が 214s+ でも継続)。原因は MCTS が 0.3s/turn budget を毎 turn フル消費 (255ms/turn) × 数百 turn。N=30×3=90 games は ~37min 見込みかつ 8-worker 並列で onnxruntime/torch メモリ圧。tournament 親プロセスが繰り返し ~466s 付近で出力前に死亡 (OOM or background-task reap)、単発 match も完走 JSON を残さず。4 approach (full tournament / workers絞り / 単発並列 / 単発1本) 全てブロック。
  - 重要 framing: **per-turn latency は submission-safe** (255ms median < 900ms、exp/050 commit で確認済) = 提出 DQ リスクは無い。問題は **ローカル mix-eval の throughput/安定性** であって agent の実行時間ではない。cycle 5 の NN-in-beam latency 警告 (~26min/6x) と同型の壁が MCTS-leaf path でも顕在化。
  - 防止策/次手: (1) NN-in-MCTS の go/no-go 統計 eval は **Kaggle 側 (episodes/replay or notebook self-play) に escalate** (supervisor gating、`local_cpu_selfplay_export_infeasible_at_scale` の eval 版)。(2) ローカルで signal が要る場合は per-turn budget を下げた縮小 config で A/B するが、submission config と乖離するため確証にならない (budget≠NN 効果の交絡)。(3) agent は byte-identical (env OFF default、提出 435.3 維持)、no-submit infra_merge で iter close。
  - 関連: `nn_value_distribution_shift_legacy388_mirror` (cycle 5 追補の latency 警告)、`local_cpu_selfplay_export_infeasible_at_scale` (data-gen 版の同型 infeasibility)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N=30 prev_best は識別力ほぼゼロ)。

- INFO: `mcts_progressive_widening_inert_small_branching` — H012 (exp/051) で MCTS progressive widening (prior=`_score_state` 上位 child を `ceil(k·N^α)` で段階解禁、env `MCTS_PW_K`) を実装し機構 probe で検証 → **PW は本 MCTS の regime で構造的 no-op**。
  - **決定的観測 (機構 probe, 5 seed dense mid-game obs)**: root child 数 (= beam frontier `_expand_turn`) は **1〜6 個**で BEAM_WIDTH=16 に程遠い。owned-planet 数が小さく、各 planet の `_candidate_actions_for_planet` も少候補のため frontier が小さい。0.3s budget で rollout は **~400 sims** 回るので child 1〜6 個に各 ~70〜400 visit = **既に過剰カバー**。visit 分布は pw_k=0/0.3/0.5/1.0 で**完全に不変** (6-child seed で全条件 top1_share=0.17 の flat、children_with>1visit=6/6)。
  - 解釈: PW は「**多数** child (continuous/large action space) に visit が薄く分散する」病に効く手法。本 MCTS は root 分岐が小さく budget が分岐を過剰カバーするため、PW の gating 効果が即座に飽和し選択 move が default と同一になる。visit 分布が flat なのは under-exploration でなく child 間 value がほぼ同値 (genuine tie) だから = learned_rules `paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` が挙げた「root-only 木で visit が薄く分散」という MCTS 残課題候補は、**少分岐ゆえ visit 不足ではなく value-tie** が実態。
  - **winrate A/B は冗長と判断 (実行省略)**: 機構 probe が visit 分布 byte-identical を示す = 選択 move が provably 同一 = winrate も exp028 (sniper 0.20/prev 0.1667) と同一になることが自明。`tournament_noise_from_wallclock_cutoff_under_parallel_load` 下で N=30 は byte-identical config すら分離できない以上、機構 probe (決定論的) の方が強い証拠。MCTS mix-eval は CPU 競合で 1 match ~3min と高コストでもある。
  - 防止策: (1) MCTS の強化は PW (分岐制御) でなく **木の深化** (root-only depth=1 → 敵応手を読む multi-ply、learned_rules で未消込の真因候補) か **eval×sigmoid scale 再設計** を優先。(2) PW/RAVE 等の large-branching 前提テクニック (H014/H015) も本 regime では同型に inert の可能性が高く、着手前に分岐数 probe で premise を確認する。(3) escape は依然 Phase 3 NN (H016, supervisor GPU/Kaggle eval escalate)。
  - 関連: `paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` (MCTS root-only 真因候補の精緻化)、`nn_in_mcts_leaf_local_eval_infeasible_in_harness` (MCTS mix-eval の高コスト)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N=30 識別力欠如)。

- AVOID: `mcts_transposition_table_inert_depth1_tree` — H014 (exp/053) で MCTS transposition table (Zobrist hash で同一 state 再訪コスト排除) の premise を**実装前に**機構 probe (`scripts/selfplay/probe_mcts_transposition.py`, 5 seed × 4 step) で検証 → **premise が構造的に成立せず inert**。`mcts_progressive_widening_inert_small_branching` の指示「PW/RAVE 系 (H014/H015) も着手前に分岐数 probe で premise を確認」を実践 (exp/051 先例)。
  - **決定的観測**: 現 MCTS (src/search/mcts.py) は root + その children のみの **depth-1 木** (探索ループ L229-242 は child を選び rollout するだけで child を孫ノードに展開しない)。各 child = root からの固有 action set 適用後 state。実測 5 seed (3/1/5/7/11) × 4 step (30/80/150/220) の **計 163 root child 全てで within-tree transposition = 0 件** (distinct_child_states == n_children always)、tt_hit_opportunity=false。
  - 解釈: TT は「同一 state を異なる経路で複数回到達 (transposition) する時、2 回目以降を cache で省略」する手法。depth-1 木では (a) 各 child が固有 action ゆえ child 間で state が重複しない、(b) 孫が無く異経路が存在しない → transposition が**原理的にゼロ**。TT は構造的に hit ゼロ = rollout/sec を上げられない。H012 PW inert と同型だが、PW が「既存 child の gating が飽和」だったのに対し TT は「対象 (transposition) がそもそも生成されない」とより根本的に不適用。
  - 補足 (切り分け): probe は phase1 rollout が決定論的 (同一 child 再 rollout が値一致) も確認。これは「同一 child を複数 sim で再 rollout すると新情報ゼロ」という別種の冗長性だが、TT (state cache) でなく rollout memoize の対象。かつ H026 (`mcts_rollout_policy_not_root_cause`) で rollout は strong-opponent regression の root cause でないと確定済のため、ここを最適化しても勝率 gain には繋がらない。
  - 防止策: (1) large-branching / 深い木を前提とする MCTS テクニック (TT / PW / RAVE=H015) は本 regime (root-only depth-1・少分岐) では全て inert。H015 (RAVE/AMAF) も着手前に同様の機構 probe で premise (action 再出現頻度) を確認すること、おそらく同型 inert。(2) MCTS を強化するなら個別テクニックでなく **木の深化 (multi-ply、敵応手を読む)** か **eval×sigmoid scale 再設計** が前提。ただし escape は依然 Phase 3 NN (H016, supervisor GPU/Kaggle eval escalate)。
  - 関連: `mcts_progressive_widening_inert_small_branching` (PW 版同型)、`paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` (MCTS root-only 構造)、`mcts_rollout_policy_not_root_cause` (rollout memoize が gain に繋がらない根拠)。

- AVOID: `mcts_rave_inert_value_tie_degenerate_rollout` — H015 (exp/056) で MCTS RAVE/AMAF (rollout で出現した action の value 統計を木全体で共有し visit 少 child を早期推定で改善) の premise を**実装前**に機構 probe (`scripts/selfplay/probe_mcts_rave.py`, 3 seed × 4 step) で検証 → **3 系統の独立理由で inert 確定**。`mcts_progressive_widening_inert_small_branching` / `mcts_transposition_table_inert_depth1_tree` の指示「H015 (RAVE/AMAF) も着手前に機構 probe で premise (action 再出現頻度) を確認、おそらく同型 inert」を実践 (exp/051 PW・exp/053 TT 先例)。
  - **P1 (visit 枯渇) 不成立**: RAVE は visit が分岐に対し不足し多くの child が 0〜数 visit の時に AMAF early-estimate が効く。実測: 分岐は dense turn で **16 (BEAM_WIDTH cap) まで到達** (H012 PW probe の「1-6」は mid-game のみ観測、後半は 16 まで増える) が、0.3s budget の ~400-2000 sim で最悪でも **avg_real_visits/child=32** (seed7 step220)、sparse では 467-2082。全 child が十分な real visit を得るため AMAF の早期推定は即 real value に上書きされ寄与ゼロ。
  - **P2 (action 再出現) は signal はあるが識別力ゼロ**: cross-child action 再出現は **非ゼロ (4-208)** で TT (transposition=0) と異なり AMAF の共有機構自体は動く。**しかし `distinct_rollout_actions=0-5`** — rollout policy が phase1 (決定論 greedy) のため、どの root child から rollout を始めても pick_best_target がほぼ同じ 1-5 個の launch に収束し action universe が退化。少数 action が全 child の rollout に一様出現する = AMAF は全 child に**同一 bonus** を与え、child 間の順位を変えられない。
  - **決定打 (value-tie, H012 追認)**: `child_value_spread=0.0-0.0068` が全 seed/step で成立 = root children は rollout value が ~3-4 桁まで同値。AMAF が child を完璧に並べ替えても、最終 argmax を決める value が tie のため**選択 move は不変**。`mcts_progressive_widening_inert_small_branching` の「visit flat は under-exploration でなく value-tie」を RAVE 文脈で再確認。
  - 解釈: MCTS 個別テクニック (PW=H012 / TT=H014 / RAVE=H015) が全て inert なのは共通根が **root children の value-tie** だから。visit 配分をどう賢くしても tie は割れない。真のレバーは (a) **eval/value の質** (children を識別できる value → Phase 3 NN H016)、または (b) **木の深化** (root-only depth-1 → 敵応手を読む multi-ply で children に differentiation を生む)。
  - 防止策: (1) large-branching/visit-scarce 前提の MCTS テクニック (PW/TT/RAVE) は本 regime (root-only depth-1・少分岐・value-tie・決定論 rollout) で全 kill 済 = 同系統の新テクニック (UCT variant, virtual loss 等) も着手前に value_spread probe で premise を確認、おそらく同型 inert。(2) escape は依然 Phase 3 NN (H016, supervisor GPU/Kaggle eval escalate) か multi-ply 深化。(3) caveat: probe は production default の phase1 rollout で実施。uniform rollout なら action universe は広がるが H026 (`mcts_rollout_policy_not_root_cause`) で uniform は strong-opponent regression を治さず production path でないため対象外。
  - 関連: `mcts_transposition_table_inert_depth1_tree` (TT 版同型・先行 probe)、`mcts_progressive_widening_inert_small_branching` (PW 版・value-tie 初出)、`paradigm_swap_alone_does_not_fix_helps_weak_hurts_strong` (MCTS root-only 構造)、`mcts_rollout_policy_not_root_cause` (phase1 rollout の決定論性)。

- AVOID: `opponent_response_modeling_lb_disproven_noop_won` — beam に「探索内で敵応手をモデル化する」改変 (defensive minimax / opponent worst-response eval 項 / opponent decision model 強化 / `_simulate_opponents` 非 noop 化) を加える hypothesis は **LB で否定済の direction** であり実装前に category-kill する。H010 (exp/057, defensive minimax を eval に組込) を実装前に discard した際に確立。
  - **決定的根拠**: 現 beam の `_simulate_opponents` は **noop** (beam.py:538-542)。この noop 化 (H025/exp029) が baseline 414.2→**435.3 (+21.1)** = 4月以来唯一の実 LB 改善の源泉。「探索内で敵を静止扱い」する方が誤った動的 opponent model で読むより LB 上優位 (`mixeval_prevbest_gate_rejected_lb_winner` と整合)。よって敵応手モデリングを**再導入**する改変は、+21 LB を生んだ軸を逆戻りさせる。
  - 既消込: H023 (`opponent_model_not_root_cause_of_depth_regression`) が「敵最悪応手 (`_opponent_best_actions` minimax)」を rollout 内で直接テスト → prev_best 0.4667 で inert/gate FAIL。eval 項 (H010) か rollout step (H023) かは表現差のみで同一方向。`deeper_search_overfits_wrong_opponent_model` (探索を深めるほど誤 opponent heuristic に過適合)、`fleet_projection_freeze_not_root_cause` (探索内 dynamics を「正確化」する方向に gain なし) も同根。
  - latency も阻害: defensive minimax は候補 state ごとに敵応手生成+評価で高コスト、dense turn は既に 0.3s budget cap 律速 (`beam_budget_underutilizes_acttimeout`)、`actTimeout_full_use_regresses_strong_opponent` で純 compute 増すら LB regression 確定。
  - 防止策: opponent-modeling 系 hypothesis (eval/rollout/classifier いずれの形でも) は priority-sort で再浮上しても着手前に本 rule で discard。Phase 1 beam 局所最適 435.3 を超える escape は (a) Phase 3 NN value 質 (H016/H017, supervisor GPU escalate)、(b) 木の深化はあくまで NN value で children を識別できる前提でのみ — を再確認。単一 knob kill 12 系統目 (opponent-response-eval)。
  - 関連: `mixeval_prevbest_gate_rejected_lb_winner` (noop +21 の一次記録)、`opponent_model_not_root_cause_of_depth_regression` (H023 minimax 応手 inert)、`actTimeout_full_use_regresses_strong_opponent` (compute 増 regression)、`mixeval_prevbest_gate_noisy_misjudges_lb` (N=30 で判定不能)。

- AVOID: `opponent_classifier_dispatch_triple_killed` — H013 (exp/058) で offline opponent classifier (expansion/aggression/comet_priority 3-class) + online dispatch を**実装前に** category-kill。opponent classifier 系 hypothesis は 3 つの独立した既消込ルールに同時抵触し、いずれか単独でも実装を正当化不能。
  - **(1) opponent-modeling = LB 否定済 (direction)**: classifier で敵タイプを推定し読み筋/戦略を変える行為は opponent-modeling であり、`opponent_response_modeling_lb_disproven_noop_won` の防止策が「eval/rollout/**classifier** いずれの形でも着手前に discard」と classifier を名指しで含む。現 beam `_simulate_opponents`=noop (敵を静止扱い, H025/exp029) が baseline 414.2→435.3 (+21.1) = 4月以来唯一の実 LB 改善の源泉。敵挙動を「より正確にモデル化」する方向は +21 を生んだ軸の逆行。
  - **(2) dispatch 先が存在しない (mechanism)**: online dispatch は「opponent type に応じて別戦略へ切替」だが、切替先候補となる beam 変種 (depth / eval-term×3 / opponent-model / fleet-projection / paradigm-swap MCTS / rollout-policy / sun-avoidance / multi-launch / anytime / budget) は単一 knob kill **12 系統で全滅**。viable な代替戦略が noop-beam 1 本のみ = 分類が正確でも切替先が全て LB regression。H019 (戦況 dynamic dispatch) が `blocked-on-phase3` とした理由と同根。
  - **(3) offline 学習データが local 生成不能 (compute)**: opponent-type ラベル付き self-play data は `local_cpu_selfplay_export_infeasible_at_scale` (~74s/game CPU、数千 game で 20h+) のため headless local iter で生成不可。Kaggle GPU/eval escalate (supervisor gating) 領分。
  - 防止策: opponent classifier / opponent-aware dispatch 系 hypothesis は priority-sort で再浮上しても本 rule で着手前 discard。H007/H010/H022 と同型の priority-sort 再浮上防止 (active→discarded)。escape は Phase 3 NN value 質 (H016/H017, supervisor GPU escalate) のみを再確認。agent byte 不変 (no code change)・no-submit・state-only merge。
  - 関連: `opponent_response_modeling_lb_disproven_noop_won` ((1) の親、classifier 名指し)、`opponent_model_not_root_cause_of_depth_regression` (opponent model 強化が inert)、`local_cpu_selfplay_export_infeasible_at_scale` ((3) data-gen 不能)、`mixeval_prevbest_gate_noisy_misjudges_lb` (仮に実装しても N=30 で判定不能)。
